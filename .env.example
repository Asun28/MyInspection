# 复制为 .env 使用（.env 不入库）。按你项目实际需要增删键。
# 这些键仅由「配置单点」（如 backend/app/core/config.py，例）读入一次；业务代码禁散读 os.environ/os.getenv（见 backend/README.md「分层 / 配置 / 注入」）。
APP_ENV=local
HOST=127.0.0.1
PORT=8000

# 运行时根目录（与中文用户名 / OneDrive 路径解耦）。
RUNTIME_DIR=runtime
TEMP_DIR=runtime/tmp

# 示例：provider / 引擎选择（若项目有 mock/real 切换）
# PROVIDER_PROFILE=mock

# 示例：商用 LGPL ffmpeg 的绝对路径（若项目分发 ffmpeg；见 docs/LICENSE-POLICY.md 附录 A）
# IMAGEIO_FFMPEG_EXE=

# AGENTS.md

## Codex / GPT-6 Astra 协作补充

本节仅补充本项目内 Codex 的沟通与执行偏好；项目架构、硬边界、关键不变量和交付流程仍遵循下方 `CLAUDE.md` 入口及其权威文档，冲突时以项目契约为准。

### 沟通

- 面向用户的叙述默认使用简体中文；代码、命令和技术标识保持英文。机器输出和 R3 评审语言遵循对应契约。
- 先给影响与结论，再给行动、待决策和必要证据；没有对应内容就省略。
- 使用简洁、连贯的段落；只有确实适合并列比较或按步骤执行时才使用列表。
- 使用具体、简单的词，避免无意义术语、套话、重复总结和未经请求的对比。
- 技术细节只保留对理解结论、判断风险或复现结果有帮助的部分。

### 指令优先级

- 遵守系统、开发者及平台指令和安全约束；本文件不能扩大工具权限或绕过审批。
- 在上述约束内，用户当前明确指令优先于 Skill 指南、历史记忆和默认偏好。
- 项目级 `AGENTS.md` 只在项目范围内补充或覆盖全局偏好；更深目录的指令按其适用范围生效。
- 如果 Skill 导致请求确认、暂停或偏离用户目标，说明具体来源，链接对应 `SKILL.md` 并引用相关条款，区分明确要求与自己的解释。

### 执行方式

- 用户表示要开始新工作或修复现有问题时，在已授权范围内持续推进，直到目标完成；遇到真实阻塞时说明已完成内容、缺失条件和下一步。
- 提问前先调查可从仓库发现的信息，并推进不依赖答案的已授权工作；需要批准时，准备具体、可检查的结果，不重复确认已授权事项。
- 歧义会实质改变结果时尽早澄清；重大设计或难逆操作遵循项目既有确认流程。例行、可逆且范围明确的动作直接执行。
- 用户建议不适合目标时，直接说明原因并给出更合适的做法。
- 不因假想风险额外添加警告、免责声明、审批流程或安全/合规清单；项目明确要求的报告免责声明、合规校验及安全闸门照常执行。
- 多步工作简要更新已完成内容与下一步；恢复会话时先核对任务状态、改动和验证证据。

### 测试与验证

- 不为可逆、影响小的改动机械新增只复述实现、没有行为验证价值的测试；功能与缺陷修复仍按项目 TDD 和验收契约执行。
- 跑与改动相称的测试，并完成项目要求的检查。通过后，只有出现新改动、新失败或尚未解决的疑点时，才扩大或重复测试。
- 声明完成前核对实际 diff 与检查结果；未运行或未通过的检查如实说明，不把工具返回成功当作验收证据。
- 收尾只清理本次产生且确认可丢弃的临时文件；保留交付物、必要验证证据和用户已有文件。

### 工具与并行

- 搜索文件或文本优先使用 `rg`、`rg --files`；独立的读取和查询尽量批量执行，依赖步骤和共享状态写入按顺序执行。
- 优先使用可用的专用工具、连接器或 CLI/API；网页控制台缺少合适接口时，使用已授权且可控制的登录浏览器，偏好 Chrome。飞书任务在 `lark-cli` 已安装并配置时优先使用。
- 在当前平台与项目规则允许时，只有存在真正独立的工作流，且委派能节省时间或提升质量，才使用子 Agent。
- 共享状态、连续决策和简单任务由当前 Agent 直接完成；委派任务须有明确输入、输出和完成判据，最终结论由主 Agent 汇总并验证。

### 规则来源

- 全局偏好由当前生效的全局 `AGENTS.md` / `AGENTS.override.md` 维护；本节只增加项目内协作偏好。
- 本仓保留 `CLAUDE.md` 作为项目规则的权威入口；项目事实、生产状态、历史决策和对外契约以该入口指定的脚本、探针、决策记录和合同文件为准，不在本节复制正文。
- 官网核验（2026-09-05）：[GPT-6 Astra 提示指南](https://developers.openai.com/api/docs/guides/latest-model#prompting-best-practices)支持自主推进、明确 Skill 优先级、简洁表达、按需委派和适量验证；[Codex AGENTS.md 文档](https://learn.chatgpt.com/docs/agent-configuration/agents-md)说明指令文件的加载与作用域。中文、Chrome、飞书和仓库入口安排属于本项目偏好。

---

> **本文件是给「读 AGENTS.md 约定」的工具（Codex / OpenAI 系）的入口指针。**
> 本仓的**唯一权威 agent 指令**是 [`CLAUDE.md`](./CLAUDE.md)——架构、硬边界、关键不变量、经验铁律、工作准则全在那里。
> Codex 作为 R3 第二评审者运行时，请先读 `CLAUDE.md` 的「硬边界 / 关键不变量」与 `docs/QUALITY-RUBRIC.md`（评审 rubric）。

## 最小约定（详见 CLAUDE.md）
- 改动前读 `CLAUDE.md`「权威文档（按序读）」索引；新文档进 `docs/` 或 `specs/`，**不在仓库根新建文件**。
- 评审/验收的确定性闸门：`scripts/verify.ps1`（CI）、`scripts/review.ps1`（Codex R3，按 `docs/QUALITY-RUBRIC.md` 判）、`scripts/selftest.ps1`（元仓自检）。
- 依赖在上下文里的库文档放 `docs/references/`（`*-llms.txt`）；**不在上下文里的东西，对 agent 等于不存在**。

# 房产巡检 App — 需求收口(Android 版)

版本:2026-09-06 · 状态:核心模块已部分实现，生产接线与发布验收推进中；实际状态以任务卡为准
标记说明:**[定]** = 已决定 · **[待]** = 需要你确认 · **[验]** = 需要 spike 验证

---

## 1. 目标与边界

**[定]** 自用工具。唯一用户是我自己,不是产品,不给其他房东用。

**[定]** 解决的问题:自管房产时,按成熟模板逐项走查、拍照、备注,生成一份专业的巡检报告,并给出整改建议。

**[定,2026-09-02]** 房客不是本 app 的用户。房客只收到经过房客受众过滤的 PDF 或自包含 HTML,不签字、不登录、不提交任何东西。

### 明确不做(写死,防止范围蔓延)

租金与账务 · 房客筛选与背调 · 维修工单派发给第三方 · 房源广告 · 押金托管 · 任何账号体系 · 任何需要服务端的功能 · 多用户与权限 · 模板编辑器 UI

---

## 2. 平台与技术栈

**[定]** Android 手机,单设备。不做 iOS,不做 Windows,不做多端。

理由:现场真正好用的相机在手机上;单设备下「同步」可以合法地推迟;多端 + 无同步 = 数据孤岛,比任何单端都差。

**[定]** 纯 Web / PWA 已排除。三条硬伤:浏览器存储配额可被系统回收(照片是跨年证据,不能放这);Web Speech API 在 Android 上依赖联网;拿不到文件系统,无法把数据目录指向云盘。

**[定,ADR-0001]** 原生 Kotlin + Compose，保留 `:core` 与 `:app`。技术选型已完成；spike 验证平台能力，不重新决定技术路线。

**[验]** 开发已进行，尚未完成的平台证据由 `T1-SPIKE-PLATFORM` 按三项补齐，不重开技术选型，也不预设一天内可完成：

1. 相机历史照片叠层：预览对位 → 拍照存入 app 私有目录 → 读回显示；如果无法成立，则记录降级为「拍完并排比对」。
2. SAF：选目录、持久授权、写入读回，并验证重启 app 后授权仍有效。
3. PDF 压力：80 张占位图与一页中英混排文本，记录真机峰值内存、耗时及字形结果。

三项各须记录设备/build、操作、截图或参数、实测结果和成立/降级结论到 `docs/spike/PLATFORM-SPIKE.md`，不能以编译成功代替。V2 录音/听写由专属卡验证，不阻塞 V1。

### 以后的多端(不在本期)

真正做了同步之后再考虑。届时 Windows 端的角色不是采集,而是**模板编辑 + 报告归档 + 历史浏览**,和手机不重叠。

---

## 3. 巡检类型

四种全做。引擎共用,差别只在模板内容。

| 类型 | 场景 | 对照基线 |
|---|---|---|
| **Routine** | 租赁定期巡检(主场景) | 上一次 Routine |
| **Ingoing** | 入住条件报告 | 无(它自己就是基线) |
| **Exit** | 退租条件报告 | 该 tenancy 显式选择的 baseline；优先 Ingoing，既有在租可指定已完成 Routine |
| **Annual Home Check** | 自住房年检 | 上一次年检 |

**[定]** Exit 以 `tenancy.baseline_inspection_id` 对照，不自动使用时间上最近的 Routine。无基线时给出不阻断的缺失警告；指定 Routine 时准确标示“Routine 基线／无入住记录”，不得显示“无基线”。下次换租客仍应建立 Ingoing。

**排程:** 默认一年 4 次(约每 13 周)。这是提醒节奏,不是法律要求。

---

## 4. 模板与数据模型

### 层级

```
物业 → 巡检(类型/日期/tenancy) → 区域 → 房间 → 检查项 → 状态 + 照片[] + 备注 + 音频(V2 采集；保留既有数据结构)
```

### 硬性要求

- **检查项必须有稳定 ID**,历史对齐靠 ID 不靠名字
- **模板带版本号**,改措辞不改 ID,加项目给新 ID
- 主键用 **UUIDv7**(不是自增整数,以后同步时自增 ID 是死局)
- 每张表有 `updated_at`(UTC)和 `deleted_at`(软删除)
- 照片存文件系统,数据库只存相对路径 + 内容哈希;**不要塞 BLOB**

### 存储量估算

单次巡检约 15–30 张照片 × 4 次/年 × N 套房。加音频。三年下来是几个 GB 量级。

**[定,2026-08-19]** 照片与 PDF 的质量是两个独立设置：

- 新拍/新导入照片：`Low = 1280px/q75`、`Medium = 1920px/q82`、`High = 2560px/q88`、
  `Extra High = 4096px/q92`，默认 `Medium`；同一个持久设置由拍摄与导入在每次操作开始时读取一次并冻结，
  EXIF 转正后按比例缩小且绝不放大小图；只影响以后写入的照片，不重压已 finalize 的证据
- 每次导出 PDF：`Low / Medium / High / Extra High`，默认 `Medium`；`High` 作为证据归档建议档，不能用一个承诺的 MB 上限代替固定夹具实测
- 当前所谓“原图”是 EXIF 转正后保存的 JPEG，不是相机 RAW/DNG；不再额外保留一份未处理原图

### 评级体系 **[定]**

同一 status 字段按模板类型限制合法值：租赁 `GOOD / FAIR / POOR / N_A`；Annual `NO_ISSUE / MONITOR / MAINTENANCE_ITEM / SIGNIFICANT_DEFECT / NOT_APPLICABLE`。UI 文案服从模板，不重新发明枚举。

### 「正常损耗 vs 损坏」 **[定]**

独立 `wear_or_damage` 三态，只用于 Exit 与所选基线有差异的项目；不增加全量双刻度、责任方或费用字段。

---

## 5. 采集流程

### 拍照规则 **[定]**

按已定模板规则分两级，避免 100 项 = 100 张:

- **房间级:强制 1–2 张全景**。ghost overlay 主要用在这里,机位一致性在全景上价值最大
- **项目级:仅不利发现强制拍照**。租赁 FAIR/POOR，Annual 的 MONITOR/MAINTENANCE_ITEM/SIGNIFICANT_DEFECT；N_A/NOT_APPLICABLE 不逼拍照，具体完备性由模板与核心返回。

这样一次 Routine 约 15–30 张,现场走得完,而全景序列反而最能看出「这几个月房子变成什么样」。

### 导入照片 **[定,已降级]**

保留,但从「核心补救手段」降为可选(手机本身就是拍照设备了)。

**[定,2026-09-06]** V1 保留现场拍摄与单项导入；批量导入与逐张分配安排 V1.1，用于集中补录旧照片，不阻塞 V1。批次提交、取消/重启保留策略和资源上限待该卡澄清；“选 20 张”只是示例，不是默认限额。

必须存:
1. **EXIF 拍摄时间**,与巡检时间分开存
2. **来源标记**(`camera` / `imported`)
3. **内容哈希**,防重复导入

哈希身份保持双轨：相机照片的 `content_hash` 覆盖缩放/编码后的最终 JPEG；导入照片仍以用户选择的原始
source bytes 作为 `content_hash`，另用 staged digest 校验派生 JPEG，质量档切换不得改变导入去重身份。

两个坑:**EXIF 旋转必须处理**(不处理导入后全是倒的);**复制不移动**,不动原文件。

导入的照片同样进入该项目的历史序列,可当下次的叠层底图。

### 导入既有 Routine DOCX 报告 **[定,2026-09-02]**

- 从物业页经系统文件选择器选一个 `.docx`；所选物业、租约、报告日期与当前内置 Routine 模板是权威，源文件里的地址、姓名、作者、URL 或签名不创建、切换或覆盖记录。
- DOCX 先作为敌意输入读入无写 staging。每个源 room/item/comment/photo/caption 必须映射、带理由排除或保持阻塞；空白/未知状态、模糊照片关系和未确认隐私分类不得静默提交。精确状态建议仍是 blocker，只有用户在完整预览后逐项或一次显式批量确认才成为 terminal `CONFIRMED`；带理由的 `EXCLUDED` 是另一 terminal 状态。
- manifest 完整、所有行 terminal、预览仍有效且零 blocker 后，才以 staged media + 单事务创建普通、可编辑的 `ROUTINE DRAFT`；已有 active draft 时拒绝，不 auto-finalize。缺失的当前模板项保持未评级，继续走正常 Capture/Review/finalize。Routine v2 是新建/导入的确定性当前版本，v1 只为历史重渲保留。
- 进程死亡时清理 source grant/staging/manifest/mapping，保留用户已确认的物业/租约/报告日期/模板并回到 `Choose file`；不得声称恢复审核决定。若 draft/receipt 原子事务已提交，则用 recovery marker 验证后只进入该普通草稿。实现测试须逐阶段注入进程死亡，证明没有半草稿或遗留媒体。
- `GEN-SUMMARY-01` 是 Routine v2 的真实“总体状况与巡检摘要”检查项；用户必须确认其正常状态枚举，源 summary 才可进入该项 note（既有 native hash 域），否则明确排除。不导入 source author/attendance/organisation。v1 不保留 raw DOCX；不支持 `.doc`、PDF/HTML/OCR import 或模板编辑器。

### 草稿与完成 **[定,2026-09-02]**

- 现场没网是默认状态,全程离线可用
- 草稿自动保存,粒度按房间
- **finalize 后原始条目只读**,可追加「补充说明」条目(独立时间戳)
- PDF 与 HTML 可内嵌 native `data_hash`、semantic fingerprint 与可选 source/mapping hashes；它们各自只证明所标范围。artifact SHA-256 必须在产物关闭并重开验证后计算，只显示在外部 receipt/UI，不得循环嵌入自身 bytes，也不得冒充对原 DOCX 或对方收件的证明

### 触摸优先

大点击区、单手可达、状态用大按钮不用下拉框、尽量少键盘输入；V1 用简单预设短语，具体描述仍可用键盘，语音安排 V2。

---

## 6. 历史对比

**[定]** 范围收窄:**只做「拍的时候能看到前几次」**。

- 走到某检查项时,当前项旁显示上次的状态、备注、缩略图;可左右滑看更早几次
- **Ghost overlay**:取景时把上次照片作为半透明底图叠在预览上,对着它取景 **[验]**
- 数据模型区分两种引用:`previous_inspection`(时间上的前一次)和 `baseline_inspection`(该 tenancy 已选定的 Ingoing 或 Routine 基线)。Exit 模式默认显示 baseline

**不做:** 差异对比 PDF、只读历史报告查看器、照片自动比对。

**冷启动:** 前 1–2 次巡检这个功能是空的,要接受。

---

## 7. 预设选项与未来语音

**[定,2026-09-06]** V1 先提供简单预设短语/选项与键盘输入。短语可按检查项和状态推荐，插入后可撤销；不自动推断评级，不覆盖已有备注。V1 不提供 app-owned 录音、听写入口或麦克风权限请求。

**[定,2026-09-06]** 录音证据与离线听写安排产品 V2，分别由 `T2-AUDIO-EVIDENCE`、`T2-ONDEVICE-DICTATION` 实现；系统服务与本地模型都尚未选定，不预设 whisper.cpp。

**[定]** V2 启用语音后，无论哪个引擎，成功保存的原始音频都应保留，与检查项绑定并可回放；识别失败仍保留原件，报告不嵌入音频，不静默联网降级。延后功能不删除已有音频或冻结结构。

**[待]** 目标手机/语言、录音格式、时长/字节上限、取消片段策略和原件可被离线转写的实际能力，留给 V2 前置验证。

---

## 8. 报告输出

**[定,2026-09-02]** 输出原生 PDF（默认/证据归档）与自包含 HTML（离线阅读/打印）。通过系统 viewer/browser/文件选择器阅读与保存——**app 负责采集与验证产物,阅读不必发生在 app 里**。

**[定]** 平行双语一份(中英并列),而不是双份单语。理由:争议发生时,双方手上是同一份文件本身就有价值。

- 模板固定项(房间名、检查项、短语库、建议库)预先双语化
- 自由文本保持原语言并标注

**[定]** 界面英文单语。为唯一用户做 i18n 框架是长期税(每加一个检查项写两份字符串)。使用英文,因为模板术语以英文为准(flashing、ground moisture barrier 这类写英文不易歧义)。

**[定]** 报告分两版:
- **房东版**:含 remediation 建议、紧急度分级
- **房客版**:客观描述，不含 remediation 或内部判断

**[定]** 报告必须带免责声明:仅供一般参考,不构成法律、建筑或物业建议;标准与规则会变,行动前请咨询持牌从业者。

**[定,2026-09-02]** 两种格式只消费一次 `ReportContent`：它在序列化前完成受众/隐私过滤，并固定 identity、状态词表、房间/项目/状态/备注顺序、摘要回链、编号照片、supplements、免责声明、租客栏、remediation 可见性与完整性/来源标签。PDF/HTML 只可在分页、视觉布局和编码上不同；被排除的 bytes 不得靠 CSS 隐藏。

HTML 是单个 UTF-8 文件：无 JavaScript、表单、外部 URL/资源或运行期网络；文本/属性按上下文转义，CSS/字体/经归一化且有界的 raster images 内嵌，具 semantic headings/tables/figures、meaningful alt/caption、responsive screen CSS 与 A4 print CSS。HTML 质量固定为 `NONE`；Low/Medium/High/Extra High 只属于 PDF。

### 照片排版

每项缩略图内联 + 附录大图。几十张图的排版是真问题,PDF 生成放在原生侧比在 WebView 里打印稳定。

**[定,2026-08-19]** PDF 导出档位按用途控制照片采样，不改变报告内容、数据哈希或已存照片：

- Low：内联约 96 dpi、附录约 120 dpi
- Medium（默认分享）：内联约 120 dpi、附录约 160 dpi
- High（证据归档建议）：内联约 150 dpi、附录约 200 dpi
- Extra High：内联约 200 dpi、附录约 300 dpi

最终像素上限和 JPEG 参数以 80 照固定夹具、房间全景、铭牌小字、低光及高熵样本的真机结果为准；档位必须保持文件大小总体单调，但不承诺任意报告的绝对 MB 大小。

---

## 9. Remediation 建议

**[定]** 不做几百条规则库。自用场景下,人工写映射表不划算。

**[定]** 做法:一个 prompt + 一份「检查项 → 建议」种子对照表,用 LLM 生成。可以跑云端 API(自己的 key),生成建议时联一次网,可完全跳过。这不违背 local-first——数据始终在本地。

**[定,SECURITY §2.1]** 远程请求不发送现场备注或任何自由文本；payload 只投影版本号、版本锁定模板中的检查项 stable id、该模板允许的状态枚举和版本锁定 seed 建议码。发送前必须展示精确 canonical JSON 供确认，adapter 对字段全集、模板成员关系和长度 fail closed；地址、姓名、电话、邮箱、路径/URI、媒体引用、报告、key 与备份内容的逐类负向夹具必须证明不会进入最终请求字节。

**[定]** 风险可控的理由:我自己看得出建议靠不靠谱,不会照单执行。但——

**[定]** 建议**只出现在房东版**。房客版只保留客观描述。

**[定]** 措辞边界:「建议联系持牌电工」安全;「更换这个开关」不安全。建议的定位是**提示 + 分级 + 建议找谁**,不是诊断 + 处方。

**[定]** 分级用 NZS 4306 的缺陷分类思路,不自创。

不做:成本估算(在 NZ 会很快过时)。

---

## 10. 合规与通知(NZ 特定)

### 通知生成 **[定]**

生成 48 小时通知文本 + 一键复制。**app 自己不发送**(会引入云依赖),由我手动发送后在 app 里记录。

送达存档必须存:
- 通知**全文快照**(渲染后的实际文本,不是模板 ID)
- 生成时间、预定巡检时间、送达方式(短信/邮件/信件)
- 送达时间戳,以及计算出的提前量(小时)
- 校验结果

### 校验规则 **[定]**

保存/发通知时阻断,不可关闭,不出现在设置里:

- 4 周内不得重复 Routine 巡检(**法律上限,不是节奏建议**)
- 通知提前量 ≥ 48 小时且 ≤ 14 天
- 巡检时间落在 08:00–19:00(寄宿公寓 08:00–18:00)
- **Ingoing / Exit 不计入 4 周限额**,校验器要按类型分流

最容易踩的场景:上次发现问题、想两周后回去复检。这时 app 必须拦下来并解释——即便双方同意,复检仍需 48 小时通知且不能突破 4 周限制。

### Healthy Homes 子模块 **[定]**

五项作为独立子模块,可单独出合规快照:加热 / 绝缘 / 通风 / 水汽侵入与排水 / 挡风。

日常巡检时的复核点:地板下绝缘未被破坏、抽风扇仍能运转、地面防潮层完好。

### 法规变更 **[定架构 / 待来源策略]**

规则通过配置与手动导入更新；不依赖服务端推送。导入需先验证可信来源、领域规则及文件身份，再由用户确认激活。由 `T4-COMPLIANCE-UPDATE-TRUST` 明确信任根/版本/撤销与回退政策，`T4-COMPLIANCE-OVERRIDE-IMPORT` 实现设置入口；同源文件自带 hash 不等于发布者身份。法律 work-check 待办保持独立，不能以新配置擅自启用。

---

## 11. 数据、备份、隐私

**[定,ADR-0002]** 工作数据留在 app 私有目录；用户通过 Android SAF 自选加密备份目的地（Google Drive / OneDrive / 本地目录 / USB 等 provider）。ADR-0006 明确 supersede ADR-0002“全部活数据放 app-specific external storage”的具体落位：SQLite、设置、回执与恢复日志收紧到 credential-encrypted internal storage，只有体积大的照片/音频可放 app-specific external storage；app-private 边界、SAF provider 范围与本地数据库唯一真相源保持不变。这是可恢复的归档导出，不宣称双向同步，云 provider 失联不得影响巡检、finalize、历史或本地 PDF。

**[定]** 单设备 = 单点故障。手机丢了、摔了,几年的基线全没。**备份是 MVP 必需项,不是 nice-to-have。**

**[定]** 备份形态:整包导出 + 按物业导出，这是必须保留的产品范围。format v1 仅提供 full 导出；旧 v1 property 包含整库 `db.sqlite`，不隔离且拒绝恢复，不提供新建入口。产品 V1 的按物业导出由 T5-PROPERTY-RESTORE-INTEGRATION 接入一致性快照、闭包、v2 writer 与 SAF，最终对象关闭重开全验后才记录成功回执，并验证实际导出包可隔离替换恢复；缺此链不得发布 V1。ADR-0006 只批准下一版按物业范围与逐表数据闭包，不是冻结格式的版本评审；实现卡仍须完成 version review。物业包须按 ADR-0006 的每表规则比较源/staging `(PK, canonical-row-hash)` 集合，并证明逻辑引用及数据库/媒体/manifest 双向完备；所有活跃模板/定义及已引用历史版本必须保留，内置短语须按锁定版本/hash 重建，确保零巡检物业也能开始新巡检。任一遗漏、多行、改值或无规则都失败。通过后按物业恢复以隔离快照替换当前 app 数据，不做隐式合并；当前有其他数据时先提示生成全量备份。

**[定,2026-09-06]** 下文 format v2 是备份文件格式升级，与产品 V2 的语音里程碑不同；按物业备份/恢复仍属已保留产品范围，必须完成版本评审、逐表闭包与应用恢复三张卡。

**[定,ADR-0006]** 恢复矩阵：v1 `full` 全验后可整包替换；v1 `property` 因整库 DB + 不完整媒体必须拒绝；完成冻结契约评审后的 v2 `full` 整包替换，v2 `property` 全验后以隔离快照替换当前 app 数据。旧 reader 拒绝 v2；新 reader 仅接受 v1 full 与 v2 full/property。未知 scope、未来格式或不支持 schema 一律拒绝，不得从标签猜测恢复语义。

**[定,ADR-0006]** 自动备份的目录树协议使用新 `.partial`，关闭重开全验后安全 rename，或复制到新最终文档并再次复核；手动 `ACTION_CREATE_DOCUMENT` 只依赖单个授权 URI，写完关闭后从同一 URI 重开全验，不假设 rename/copy。只有最终可读对象通过解密与 manifest/hash/size 校验才更新 Verified 回执；失败残留尽力删除，无法删除时提示用户且不得覆盖最近已验证状态。

**[定,ADR-0006]** 本地目录/USB 备份和本地包恢复可完全离线；Drive/OneDrive 等 SAF provider 是否离线可用由 provider 决定。provider 不可达、授权收回、低空间或后台密钥不可用，只影响该次备份/恢复，保留最近已验证回执并给出重试/重选/重新输入口令动作。

**[定,ADR-0006]** 自动备份仍为 finalize 后 + 每周，通过 Android SAF/DocumentsProvider 写入用户选择的目录；云 provider 对密文的后续传输由 provider 管理，不是 app-owned HTTP 上传。用户口令是跨设备恢复根；本机只保存由 Android Keystore 加密的口令信封供后台任务使用，不保存明文，信封不进入备份。Keystore 失效时自动备份暂停并要求重新输入口令，绝不降级成明文包。

**[定,2026-08-19]** 联系方式仍按租约结束后 12 个月一键清理。照片记录、哈希、PDF 和加密备份不按“保留几轮”删除；“保留几轮”只控制本机是否继续保留可重建的全尺寸照片字节。

本机全尺寸照片默认保留每个物业最近 **3 次已完成巡检**，用户可选 `1 / 3 / 5 / 10 / Always`。当前巡检、previous、baseline、手动保留项，以及未满足安全归档条件的照片永不进入清理候选。

安全归档条件必须同时满足：巡检已 finalize、PDF 已生成、照片的相对路径/哈希/大小已写入一份加密 `.mibk` 并从目标重新读回校验、已过 30 天宽限期、且不在上述固定集合。默认只预览并逐次确认；自动清理推迟到恢复/回填流程经真机验证后。清理后保留 DB 记录、内容哈希、PDF 和加密备份，不删除云端文件。

Google Drive、OneDrive、本地目录或 USB 通过 Android SAF 作为备份目的地；Google Photos 的“已备份”状态不作为可验证归档回执，v1 不接云账号、不做云端同步或云端删除。

**[未来设计,不在 v1]** 可增加付费托管云备份，但必须作为同一归档接口的另一种 provider，而不是把 S3 细节写进领域层。app 仍先在设备端生成加密 `.mibk`；服务端只在认证和订阅有效时签发短时上传/下载授权，S3 保存不含地址/租客名的 opaque object key 与密文。APK 不内置 AWS key，服务端不持有备份口令。云备份与多设备同步是两个产品，后者须另立方案。

**[定,ADR-0006]** 安全基线（个人项目、轻量）：数据库本身不做应用级加密，依赖 Android 文件加密、app sandbox 和设备锁；备份包必须认证加密。系统 Auto Backup/设备迁移排除全部 app 数据；不做云端账号、同步、遥测或 app-owned 后台 HTTP 上传。远程 remediation 是唯一 app-owned 联网点，必须显式触发、payload 最小化，离线失败不影响核心流程；自动 SAF 写入及 provider-managed 密文传输继续允许。

---

## 12. 当前待澄清与已确认事项

已确认：Kotlin+Compose、租赁四态/Annual 五态、Exit 差异项 wear/damage、N_A 不逼拍照、finalize 只读与追加补充、英文 UI/双语报告、2 套以上物业部分在租、联系方式 12 个月，以及 V1 预设/键盘、V1.1 批量照片、V2 语音。不得重新当作未决需求。

仍待澄清：规则可信来源/轮换/日期和回退政策；remediation provider/key 与请求策略；冻结 format v2 的具体布局；V1.1 批量提交/取消/重启/上限；V2 录音和离线识别参数。法律 s48(2)(c) work-check 的既有待办保留。本轮没有替用户选服务商、信任根或默认限额。

发布范围与闭合标准见 TASK-BOARD 的“2026-09-06 审校补卡与版本计划”。没有实测证据的能力不得以库测试通过标记为 App 发布可用。

---

## 13. 建议的实施顺序

1. **Spike**:验证相机叠层、SAF 与 PDF 真机能力；技术路线已经确定
2. 数据模型 + SQLite schema(检查项稳定 ID、模板版本、UUIDv7)
3. Routine 模板内容(约 80–120 项,双语)—— 这是纯抄写,但抄错不报错
4. 生产装配与采集流程 + 拍照/单图导入 + 简单预设短语/键盘（语音 V2）
5. PDF/HTML 双受众输出、验证与分享；DOCX 导入普通可编辑草稿
6. 历史对比 + ghost overlay
7. 通知生成 + 校验引擎 + 送达存档
8. 备份/导出
9. Ingoing / Exit / 自住年检模板
10. Remediation 建议

---

## 14. 生产可用性与非功能验收 **[定,2026-08-20]**

### 安全与隐私

- 图片导入只使用 Android Photo Picker/SAF，禁止申请全相册权限；app 不读取剪贴板，只有用户点 `Copy notice` 才写入通知文本。
- 密钥和凭据进入 Keystore 支持的本机加密存储；系统备份/设备迁移不得携带 app 数据；敏感文件只用临时只读 `content://` 分享。
- 本产品没有账号，故“账号物理注销”等价为 `Delete all local data`：二次输入确认后以可恢复擦除标记清除本机数据库、媒体、报告、设置、密钥、诊断、缓存与授权；诊断库最后删除，成功不写日志、不重建数据库并回到 first-run，取消/前置失败只在诊断存储仍存在时记录。用户自行保存的外部加密 `.mibk` 不删除且确认前明确说明。
- Remediation 是唯一联网点（仅指 app-owned HTTP adapter）：显式触发、最小 payload、HTTPS/平台 TLS、禁止 cleartext 和自定义信任绕过。自动 SAF 写入与 provider-managed 密文传输不属于 app 网络 adapter。无自有服务端时不得伪造 Timestamp+Nonce 防重放或额外引入 HTTPDNS；若未来新增可验证服务端/解析信任边界，必须先立 ADR 与威胁模型。

### 本地监控与发布证据

- 核心操作写入有界、脱敏、本机诊断事件；事件失败不影响业务。v1 不做 app-owned 自动遥测、Wi-Fi 诊断上传或远程秒级告警，用户只在明确确认后导出诊断包；这不禁止已授权的自动 SAF 加密备份。
- finalize/PDF/备份/恢复失败、连续 3 次备份失败、备份超过 7 天、完整性失败、恢复回滚和上次崩溃恢复必须在本机 1 秒内形成可操作状态提示。PDF 使用 registry 的 `PDF_GENERATE` 封闭事件并按巡检与房东/房客版本分别清除；事件先后只按 UUIDv7-keyed counter 分配的唯一单调序列（不是主键），活动告警投影不得因 90 天/20,000 行事件裁剪而消失。
- 崩溃记录按 Diagnostic registry v1 保存 `PREVIOUS_CRASH/FAILURE`、封闭来源/reason、格式受限 build id、封闭异常类型和最多 8 个安全帧标识；Android 11+ 只接受明确异常的系统退出原因，Android 10 及以下只接受 app 自写的未捕获 Java 异常 marker，正常回收/用户停止/升级不告警且同一退出只消费一次。不保存系统 description/trace、message、原始类名、行号、路径、URI 或业务值。每个 release 保留对应 Android mapping/符号证据与校验值，不把符号表、崩溃或日志自动上传。
- 发布前必须完成权限拒绝、飞行模式、进程死亡、备份/恢复、数据清除、崩溃恢复和临时分享授权矩阵。

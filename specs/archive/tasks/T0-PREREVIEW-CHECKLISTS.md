---
id: T0-PREREVIEW-CHECKLISTS
title: docs/PREREVIEW-CHECKLISTS.md - worker-facing checklists in four Lens sections that enter the pack and the policy hash
status: merged
depends_on: [T0-PREREVIEW-SCHEMA]
allow_paths:
  - docs/PREREVIEW-CHECKLISTS.md
  - scripts/fixtures/prereview/checklists/
  - specs/tasks/T0-PREREVIEW-CHECKLISTS.md
dod_command: $f = 'docs/PREREVIEW-CHECKLISTS.md'; if (-not (Test-Path $f) -or (Get-Item $f).Length -gt 12288) { exit 1 }; $l = @(Get-Content $f); foreach ($s in @('code', 'tests', 'prose', 'scripts')) { if (@($l | Where-Object { $_ -ceq ('## Lens: ' + $s) }).Count -ne 1) { exit 1 } }; $c = @($l | Where-Object { $_ -cmatch '^- C[1-7] ' }); if ($c.Count -lt 8 -or @($c | Where-Object { $_ -cnotmatch 'L[0-9]+|rubric:[0-9]+' }).Count -ne 0) { exit 1 }
dod_exit: 0
dod_assert: docs/PREREVIEW-CHECKLISTS.md exists, is at most 12288 bytes, has exactly one line each case-sensitively equal to ## Lens: code, ## Lens: tests, ## Lens: prose and ## Lens: scripts, and every check line starting with - C{n} (at least eight of them) names a lesson id L{digits} or a rubric dimension rubric:{digits}, all matched case-sensitively.
review_gate: codex {verdict:pass}
plan_ref: docs/TASK-BOARD.md#pr-review-v2
parallelizable_with: [T0-PREREVIEW-RUNNER, T0-PREREVIEW-FACTS-EXTRACT, T0-PREREVIEW-FACTS, T0-PREREVIEW-PROTOCOL-DOC, T0-PREREVIEW-RECORDS, T0-PREREVIEW-FACTS-LIB, T0-PREREVIEW-WORKERS, T0-PREREVIEW-WORKERS-CLAUDE, T0-PREREVIEW-WORKERS-DEEPSEEK, T0-PREREVIEW-STATE-1A]
acceptance:
  - "A1 docs/PREREVIEW-CHECKLISTS.md is at most 12288 bytes and has exactly one heading line each for Lens code, tests, prose and scripts in the form Lens: {class}, which Select-PrereviewLensSections (PROMPT) selects by path class."
  - "A2 Every check line has the form - C{n} ... with n in 1..7 and names at least one source token L{digits} or rubric:{digits}, so each check is traceable to a lesson or rubric dimension and is machine-checkable."
  - "A3 The code section covers guards on every entry point, empty/null/overflow, fail-open branches, ordering assumptions and API level versus minSdk; tests covers L165, L225 and L282; prose covers L309, L317, L321 and L224 plus the sibling-clause and universal-claim checks; scripts covers every error branch open or closed, every exit code reachable and every ASCII code unique."
  - "A4 A coverage-rules paragraph states that the discoverer returns one coverage record per unit in units.json with categories_checked covering C1, C2 and C3 (RECORDS synthesises missing otherwise), that candidates use only C1, C2, C3, C4, C5, C7, and that C6 is derived from dispute records only."
  - "A5 The document is written as data: it enters the pack as checklists.md and the policy hash with the record schema (FACTS-LIB), so any later wording change is a policy change by construction; the card adds nothing outside this document."
forbid:
  - Any content outside docs/PREREVIEW-CHECKLISTS.md
  - A separate lens per path class or per-lens A/B (one composed lens only)
non_goals:
  - Replacing the codex R3 gate with a ReviewCommand pipeline backend, or feeding the packet into the codex prompt (Phase 2)
  - Codex candidate verification, DeepSeek re-check of fixes, codex bounded independent check (Phase 2, schema_version 2)
  - Incremental changed-units-only re-discovery, snapshot refs, coverage carry-forward, automatic re-open of fixed candidates
  - Vote counting, a lock file, a refs plane, run -Live overrides, a new top-level selftest gate number, an infra round cap
  - Fully qualified -Base refs, cross-file JSON schema references, changes to specs/verdict.schema.json or scripts/_gitbase.ps1
  - Any Phase-1b behaviour: dispositions, review_status and gate predicates, batch semantics, the ship gate leg, ledger rows, metrics
---

# T0-PREREVIEW-CHECKLISTS

## Context

Worker-facing checklists, written as data: the file enters the pack as checklists.md and the policy hash (FACTS-LIB), so a wording change is a policy change by construction.

## Sections

- Lens code (android/** production): guards on every entry point, empty/null/zero/negative/overflow inputs, fail-open branches, ordering assumptions on collections feeding hashes or receipts, API level of every JDK/Android call against minSdk (rubric 15).
- Lens tests (*Test* files, selftest fixtures, receipts): L165, L225, L282.
- Lens prose (comments, KDoc, docs/**, context/**, specs/**): L309, L317, L321, L224, sibling-clause and universal-claim checks.
- Lens scripts (scripts/**, .github/**, .claude/hooks/**): every error branch open or closed, every exit code reachable, every ASCII code unique.
- Coverage rules: one coverage record per unit with categories_checked covering C1, C2 and C3; candidates use C1, C2, C3, C4, C5, C7; C6 is derived from dispute records only.

## Implementation record (2026-09-15)

- Delivered: docs/PREREVIEW-CHECKLISTS.md only (12238 bytes after R3 round 4, LF, no BOM, ASCII): a header stating the data/policy-hash property (A5) and how a check maps to a record (`- C{n}` = category, one parenthesised token = the record's `contract_ref`, section selection by path class with the own-card/_local/.review exclusion and the `review_lens` pin from PROMPT A3/A4), four `## Lens:` sections (code 9 checks, tests 8, prose 8, scripts 8; 33 check lines, categories C1 8 / C2 11 / C3 10 / C4 1 / C5 1 / C7 2, no C6), and the coverage-rules paragraph (A4). Every check line ends with a parenthesised list in the schema's `contract_ref` forms; the 25 lesson ids cited all exist as `## L<n>` headings in `git show master:docs/lessons/LEDGER.md` and the 10 rubric numbers all exist as dimension lines in docs/QUALITY-RUBRIC.md sections 1-2 (checked by script, not by reading). Nothing under scripts/fixtures/prereview/checklists/ was added.
- DoD tightened on master before ship (6881a970, card front-matter only): `-eq`/`-match`/`-notmatch` became `-ceq`/`-cmatch`/`-cnotmatch`, because the case-insensitive form accepted a lowercased heading (mutant M6 below survived it), and `dod_assert` now names the `## Lens:` form the command checks (it said `# Lens:`).
- R4 hygiene, DoD run on scratchpad copies (delivered file untouched), all red against the tightened DoD: M1 drop the tests heading; M2 duplicate the code heading; M3 strip the parenthesised refs from one check line; M4 keep only 7 check lines; M5 pad past 12288 bytes; M6 lowercase one heading (survived the original `-eq`, red under `-ceq`); M7 lowercase one lesson token (`lesson:l228`). Control: the delivered file exits 0.
- Pre-R3 fresh-context review (task-loop 4.6, one Claude sub-agent fed the card, the file, protocol sections 1/3/5/6, the frozen schema, PROMPT A3/A4, the rubric and the master ledger entries): 1 block and 7 notes, all accepted and fixed: `non_goals` overreach was filed under C7 while protocol section 6 puts it in C5 (moved, with rubric:14); a per-check `status` was implied while the schema's `status` is per unit (now "report `status` per unit, not per check"); "the tokens are the `contract_ref` values" while `contract_ref` is one string (now "one of the tokens"); the section-selection sentence lacked the own-card/_local/.review exclusion (added); two lines about tests in a source set no task runs cited L281/L19 while the ledger entry that says it is L280 (added); rubric:7 (traceability) cited for placement (dropped, rubric:1 kept); rubric:9 (error handling) cited for a guard missing on a second entry point (dropped, L228 kept); "localised text as a machine-checked assertion" sat in a C2 shell-boundary line while the protocol maps L165 to C3 (split into its own C3 line in the scripts section). Before that review, a self-pass on the draft had already narrowed four wider-than-mechanism sentences (a JVM test "passes" an API missing at the target level; "can be cast back to mutable" said of every published collection; the selection sentence without the `review_lens` pin; "copied from the implementation ... stays green").
- Routed selftest (`selftest.ps1 -TaskId T0-PREREVIEW-CHECKLISTS -Base 6881a970`, mode=all because the card body changed), run from the worktree's own copy: seeded exit 0, workflow exit 0, core exit 1 after 1489 s with exactly two failed gates, 2 and 16, both on the same pre-existing line: `CLAUDE.md` line 88 at base cites `L324`, and no `## L324` heading exists in `docs/lessons/LEDGER.md` at master (the entry lives only in the main checkout's uncommitted ledger; gate 2 prints "CLAUDE.md 引用了不存在的 L324", gate 16 prints "悬空经验引用：CLAUDE.md → L324"). `CLAUDE.md` is outside this card's allow_paths and untouched by the diff; `[SELFTEST-FAILED-GATES] shard=core gates=2,16` names no other gate, and the core skip summary is the routine 11 (post-init not-applicable 3/4/5/8/8.0c/11b/14e/14f(config-map), nightly-deferred 8.2e x2, TEMPLATE-README.md missing). The line is reworded in the R5 doc-sync commit so the next routed run is green.
- R3 round 1 (codex, `-Local`): block with one reason, accepted and fixed in place: four checks were worded wider than their cited lessons and would have produced false candidates systematically: boundary values demanded regardless of the parameter's type (now "the boundary values its type admits", listed by type); every default branch called fail-open although `default -> throw` is fail-closed (now "a default or else branch that returns a permissive value instead of throwing"); every whole-stdout assertion called weak although exact whole-output equality detects removed output (now "a substring or keyword match over whole stdout", with exact whole output named as acceptable); sentinel uniqueness demanded per emitting branch (now per documented meaning, with same-meaning branches allowed to share a code). A sweep of the other 29 lines with the same lens narrowed five more: the red-test check now applies per behavioural change and asks for at least one test; the sibling-file check names "the unit's test file" instead of assuming an existing one; the acceptance-row check fires only when a "covered/verified" phrase can point to no test, mutant or receipt; an open branch on a gate path is a defect only when the script does not document it as intended; only native calls whose result feeds a decision need an exit-code guard. A second-model pass (DeepSeek, fed the reason, the fix diff and the file) confirmed each of the four fixes matches the requested qualification and added two wording notes, both applied: "total order stated in code" became "enforced in code (a sort call or a sorted collection type counts)", and "the test" became "at least one test". File after the round: 11325 bytes, 33 check lines, DoD exit 0.
- R3 round 2 (codex, `-Local`): block with one reason on three more lines of the same class, accepted and fixed in place: "one mutant per assertion" applied to every assertion while L225 governs assertions a receipt or claim relies on (now "one mutant per claimed assertion: where a receipt, card row or DoD claims that an assertion is mutation-covered"); the sibling-file negative case demanded a "parseable-but-wrong" input for every guard although a null or empty guard cannot be reached that way (now "an input tailored to it", with the per-guard shapes listed); the acceptance-row check demanded a test and a mutant in the diff for every automated row although rubric #6 accepts a card's declared self-verifying DoD (now "the evidence shape the card declares for it: a test, a mutant, a receipt, or the card's own DoD command"). Same-lens sweep narrowed one more: "computed by calling the production function" now excludes a round trip compared with the original input. Round counter at the cap (2) after this fix; file 11753 bytes, 33 check lines, DoD exit 0. The user ruled `review.ps1 -ResetRounds` for one more real review (two rounds of distinct, accepted, fixed findings, not a repeated dispute); the counter was cleared and round 3 shipped through the same `ship -Local` path with the reviewer invoked.
- R3 round 3 (codex, `-Local`, first round after the reset): block with two reasons, both accepted and fixed in place. (1) Three more lines of the same class: the assertion-face check rejected every substring match over stdout while recommending sentinel matching in the same sentence (now "a match over the whole output that a line other than the decision line can satisfy", with "cut the decision line out by its stable identifier and match its ASCII sentinel" as the fix); the acceptance-row check required the card's DoD evidence inside the diff although the pack carries the card at base (now "present in this diff or in the card at base"); the scripts C1 line said every cause-specific failure message goes stale (now "a message that names a cause the changed code no longer detects is stale", with L97's report-the-data preference cited as a preference). (2) The coverage paragraph's status mapping (`finding` when a candidate names the unit, `checked_no_finding` otherwise) left the contract's `not_applicable` unreachable; the mapping is removed and the paragraph now only says `status` is per unit with one of the contract's four worker values and that `blocked` names its location. Because this is the third round of the same class, the fix was an instance table over every line (L309) rather than three spot edits: it also narrowed the red-test check to accept the evidence shape the card's DoD declares, the published-set check to collections whose runtime class is mutable, the undecidable-rule clause to rules stated as mandatory, the localised-text gate to captures that do not pin the encoding on both sides (L66), and the header's "one candidate per site" to one record with `unit_ids[]` naming every unit it touches. Two independent full-document sweeps with the reviewer's lens then ran before round 4 (each line: name a concrete repository case the clause would wrongly flag): the Claude sweep raised 26 (3 block, 16 note, 7 nit), grounded in named repository instances (the `SDK_INT`-guarded API-33 call in the reminder presenter, `ComplianceConfig`'s catch that returns a rejection, the `ORDER BY` feeding the snapshot hash, the `NoticeService` in-process Chinese-text equality, the `#!/bin/sh` pre-push hook that runs `check-secrets.ps1` by a forward-slash path, `e2eTest` run by `verify.ps1` but by no `dod_command`, rows marked manual that say "verified"); 24 were applied (all 3 blocks and 21 of the notes/nits, including deleting the "undecidable mandatory rule" clause from the prose C7 line, which no cited lesson supports), 2 nits skipped for budget (a "directly or on its one path" clause on the guards line; a type-made-guard clause on the sibling-file line). The DeepSeek sweep raised 12; 2 applied (evidence may live anywhere in the snapshot tree, not only the diff; "exact mechanism" implied a single location), 10 rejected because the clause already carried the qualification asked for. Bytes were funded by cutting the coverage paragraph's two sentences that restated the header and protocol section 6 and by shortening the boundary-value list; file after the pass 12209 bytes (limit 12288), 33 check lines, 25 lesson ids and 10 rubric numbers all resolving at master, DoD exit 0.
- R3 round 4 (codex, `-Local`): block with two reasons, both accepted and fixed. (1) Out of scope (rubric #1): this implementation record was being carried in the branch diff under `specs/tasks/T0-PREREVIEW-CHECKLISTS.md` while the card's forbid (`Any content outside docs/PREREVIEW-CHECKLISTS.md`) and A5 exclude anything but the document; the branch copy of the card is restored to its base version (the branch diff against the merge-base is now `docs/PREREVIEW-CHECKLISTS.md` alone) and this record lives on master's copy of the card (card metadata goes to main, L18). (2) The prose C7 line classified any second copy of a list as a finding, which would flag this document's own A4-mandated coverage paragraph; it is restricted to a copy maintained apart from its truth source or presented as authoritative, a restatement that names its source is excluded, and the coverage paragraph now opens by naming protocol section 6 as the source. Bytes funded by dropping one of two examples from the test-names line and a parenthetical from the assertion-face line; file 12238 bytes (the PROMPT-card selector name and one "red test" clause also trimmed for headroom), 33 check lines, DoD exit 0. Round counter at the cap (2) again after this fix; the user ruled a second `-ResetRounds` (round 5 is the first review of the document-only diff).
- R3 round 5 (codex, `-Local`): pass, zero findings. Merged into master `2782b55b` (merge commit of branch tip `5d46d57c`, document 12238 bytes). Five rounds; four blocks carrying 6 reasons and 13 distinct findings, every one accepted and fixed (12 in the document, 1 by moving this record to master); two user-authorised `-ResetRounds` (after rounds 2 and 4).

## Notes

At most 12288 bytes. Collision rule (authority: docs/TASK-BOARD.md section PR review v2): no worktree branch and no todo card whose changes or allow_paths touch a chain file (review.ps1, task.ps1, selftest.ps1, _config.ps1, CLAUDE.md, the task-loop skill, QUALITY-RUBRIC.md, DEVOPS-WORKFLOW.md, TRUST-MANIFEST.md, TASK-BOARD.md) may start while the chain card owning that file is open; merge or retire first. origin/master (67 ahead, 37 chain-file commits incl. #265 which delivered T0-CI-DEADLINE-CONTAINMENT upstream) is not reconciled into master while any 1a chain card is in flight; reconcile before T0-PREREVIEW-RUNNER starts or after T0-PREREVIEW-LINK-RECALL merges, by user decision, after closing the local T0-CI-DEADLINE-CONTAINMENT card against #265.

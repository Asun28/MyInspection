---
id: T0-RECONCILE-DESIGN-FOUNDATION-R3-PAIR-FIXTURE
title: 登记 Foundations 精确渲染配对与层级修订源
depends_on: [T0-RECONCILE-DESIGN-FOUNDATION-R3-FIXTURE]
status: merged
branch: T0-RECONCILE-DESIGN-FOUNDATION-R3-PAIR-FIXTURE
worktree: C:\\wt\\T0-RECONCILE-DESIGN-FOUNDATION-R3-PAIR-FIXTURE
allow_paths:
  - specs/tasks/T0-RECONCILE-DESIGN-FOUNDATION-R3-PAIR-FIXTURE.md
  - specs/tasks/T0-RECONCILE-DESIGN-FOUNDATIONS.md
  - specs/tasks/T0-RECONCILE-DESIGN-COMPONENTS.md
forbid:
  - 修改产品代码、主设计正文或 R3 预算
  - 改写既有源提交历史或删除组件合同
non_goals:
  - 执行 Foundations 或 Components 卡
diagnosis: PR 165 的后续 R3 发现 d69ca4b 仅按前景 token 判覆盖，未验证证据轨与必要卡片边界的精确渲染配对，并把 grouped region 与 active card 的 tonal level 顺序写反；原卡 DoD 固定旧源，必须先登记新后继。
acceptance:
  - "A1 9bdcf96 是远端夹具分支上 d69ca4b 的直接单文件后继"
  - "A2 源含 49 个唯一 bindings，并验证 12 个证据轨/卡片精确渲染配对"
  - "A3 grouped region → active card 的 tonal level、token 与边界文案一致"
  - "A4 Foundations 与 Components 两卡全文固定并共同改钉新源"
dod_command: pwsh -NoProfile -File scripts/check-cards.ps1;if($LASTEXITCODE-ne0){exit 1};$oid='9bdcf966d4beb676e286175158f5b17175ae8f71';$old='d69ca4bf91abcfd6c021399aa1bba7d2cd62680c';$remote=&git ls-remote --exit-code origin 'refs/heads/codex/design-metadata-fixture-v2'|Out-String;if($LASTEXITCODE-ne0-or[regex]::Match($remote,'^[0-9a-f]{40}').Value-cne$oid){throw 'durable remote ref'};$parent=(&git rev-parse ($oid+'^')).Trim();if($LASTEXITCODE-ne0-or$parent-cne$old){throw 'direct parent'};$paths=@(&git diff-tree --no-commit-id --name-only -r $oid);if($LASTEXITCODE-ne0-or$paths.Count-ne1-or$paths[0]-cne'context/DESIGN.md'){throw 'fixture paths'};$stat=(&git diff-tree --no-commit-id --numstat -r $oid).Trim();if($LASTEXITCODE-ne0-or$stat-cne("29`t7`tcontext/DESIGN.md")){throw ('fixture stat '+$stat)};$src=(&git show ($oid+':context/DESIGN.md')|Out-String).Replace([string][char]13,'');if($LASTEXITCODE-ne0){throw 'source'};$region=[regex]::Match($src,'(?ms)^## Colors\n.*?(?=^## Components\n)').Value;$m=[regex]::Match($region,'(?ms)^### CI contrast gate metadata\n.*?```json\n(.*?)\n```');if(-not$m.Success){throw 'metadata'};$j=$m.Groups[1].Value|ConvertFrom-Json -Depth 20;if($j.schemaVersion-ne2-or$j.bindings.Count-ne49){throw 'metadata shape'};$pairs=@($j.bindings|%{$_.foreground+'|'+$_.background});if(@($pairs|sort -Unique).Count-ne49-or@($j.bindings|?{$_.foreground-like'light.*'}).Count-ne24-or@($j.bindings|?{$_.foreground-like'dark.*'}).Count-ne24-or@($j.bindings|?{$_.foreground-like'camera.*'}).Count-ne1){throw 'binding set'};$ns=$j.namespaceResolution;$nsNames=@($ns.psobject.Properties.Name);$wantNs='light.<role>,dark.<role>,camera.scrim,camera.on-scrim,camera.scrim-over-white'-split',';if($nsNames.Count-ne$wantNs.Count-or(Compare-Object ($nsNames|sort) ($wantNs|sort))){throw 'namespace set'};if($ns.'light.<role>'-cne'frontmatter.colors.<role>'-or$ns.'dark.<role>'-cne'frontmatter.dark-colors.<role>'-or$ns.'camera.scrim'-cne'frontmatter.interaction.cameraScrim'-or$ns.'camera.on-scrim'-cne'frontmatter.interaction.onCameraScrim'-or$ns.'camera.scrim-over-white'-cne'alphaCompositeSrgb(camera.scrim, 0.64, #FFFFFF)'){throw 'namespace values'};$fm=[regex]::Match($src,'(?s)^---\n(.*?)\n---').Groups[1].Value;function TokenMap($section){$block=[regex]::Match($fm,('(?ms)^'+[regex]::Escape($section)+':\n(.*?)(?=^[a-z][a-z-]*:\n)')).Groups[1].Value;if(-not$block){throw ('token block '+$section)};$h=@{};foreach($v in [regex]::Matches($block,'(?m)^  ([A-Za-z0-9-]+): "(#[0-9A-F]{6})"$')){$h[$v.Groups[1].Value]=$v.Groups[2].Value};$h};$light=TokenMap 'colors';$dark=TokenMap 'dark-colors';$camera=TokenMap 'interaction';if($light.Count-ne28-or$dark.Count-ne28-or$camera.Count-ne2){throw 'token map shape'};$interactionBlock=[regex]::Match($fm,'(?ms)^interaction:\n(.*?)(?=^motion:\n)').Groups[1].Value;$opacityMatch=[regex]::Match($interactionBlock,'(?m)^  cameraScrimOpacity: ([0-9.]+)$');if(-not$opacityMatch.Success){throw 'camera opacity'};$opacity=[double]::Parse($opacityMatch.Groups[1].Value,[Globalization.CultureInfo]::InvariantCulture);function ResolveToken($name){if($name-match'^light\.(.+)$'){$v=$light[$Matches[1]]}elseif($name-match'^dark\.(.+)$'){$v=$dark[$Matches[1]]}elseif($name-ceq'camera.scrim'){$v=$camera.cameraScrim}elseif($name-ceq'camera.on-scrim'){$v=$camera.onCameraScrim}elseif($name-ceq'camera.scrim-over-white'){if($camera.cameraScrim-cne'#000000'-or$opacity-ne0.64){throw 'camera composite inputs'};$ch=[int][Math]::Round(255*(1-$opacity),0,[MidpointRounding]::AwayFromZero);$v=('#{0:X2}{0:X2}{0:X2}'-f$ch)}else{throw ('unresolved token '+$name)};if(-not$v){throw ('missing token '+$name)};$v};$allow=@($j.pureColorAllowlist);$want='light.on-primary,light.on-secondary,light.on-tertiary,light.surface-container-low,light.on-error,light.on-privacy,camera.scrim,camera.on-scrim'-split',';if($allow.Count-ne$want.Count-or(Compare-Object ($allow|sort) ($want|sort))){throw 'pure allowlist'};foreach($name in $allow){if((ResolveToken $name)-cnotin @('#000000','#FFFFFF')){throw ('non-pure allowlist '+$name)}};function Lum($hex){$rgb=1,3,5|%{[Convert]::ToInt32($hex.Substring($_,2),16)/255};$lin=$rgb|%{if($_-le.04045){$_/12.92}else{[Math]::Pow(($_+.055)/1.055,2.4)}};.2126*$lin[0]+.7152*$lin[1]+.0722*$lin[2]};foreach($b in $j.bindings){$fg=ResolveToken $b.foreground;$bg=ResolveToken $b.background;if($fg-cne$b.value-or$bg-cne$b.backgroundValue){throw ('binding resolution '+$b.foreground+'|'+$b.background)};$xv=Lum $fg;$yv=Lum $bg;$ratio=([Math]::Max($xv,$yv)+.05)/([Math]::Min($xv,$yv)+.05);if($ratio+0.0001-lt$b.minRatio){throw ('binding ratio '+$b.foreground)}};$darkRows=@();foreach($row in [regex]::Matches($region,'(?m)^\| `(dark\.[a-z0-9-]+)`(?: focus ring)? \| `(#[0-9A-F]{6})` \| `(dark\.[a-z0-9-]+)` \| `(#[0-9A-F]{6})` \| `([0-9]+\.[0-9]+):1` \|')){$key=$row.Groups[1].Value+'|'+$row.Groups[3].Value;$darkRows+=$key;$binding=@($j.bindings|?{($_.foreground+'|'+$_.background)-ceq$key});if($binding.Count-ne1-or$binding[0].value-cne$row.Groups[2].Value-or$binding[0].backgroundValue-cne$row.Groups[4].Value){throw ('dark row binding '+$key)};$xv=Lum $row.Groups[2].Value;$yv=Lum $row.Groups[4].Value;$ratio=([Math]::Max($xv,$yv)+.05)/([Math]::Min($xv,$yv)+.05);if($ratio.ToString('F2',[Globalization.CultureInfo]::InvariantCulture)-cne$row.Groups[5].Value){throw ('dark row ratio '+$key)}};$metadataDark=@($j.bindings|?{$_.foreground-like'dark.*'}|%{$_.foreground+'|'+$_.background});if($darkRows.Count-ne24-or@($darkRows|sort -Unique).Count-ne24-or(Compare-Object ($darkRows|sort) ($metadataDark|sort))){throw 'dark map set'};$decl=@($j.bindings|%{$_.foreground+'|'+$_.background});$front=[regex]::Match($src,'(?s)^---\n(.*?)\n---').Groups[1].Value;$cb=[regex]::Match($front,'(?ms)^components:\n(.*)\z').Groups[1].Value;$covered=0;foreach($block in [regex]::Matches($cb,'(?ms)^  ([a-z0-9-]+):\n(.*?)(?=^  [a-z0-9-]+:|\z)')){$id=$block.Groups[1].Value;$body=$block.Groups[2].Value;$refs=@([regex]::Matches($body,'(?m)^    ([A-Za-z0-9-]*Color): "\{(colors|interaction)\.([A-Za-z0-9-]+)\}"$'));if(-not$refs.Count){continue};$covered++;$by=@{};foreach($ref in $refs){$by[$ref.Groups[1].Value]=$ref.Groups[2].Value+'.'+$ref.Groups[3].Value};if($id-ceq'evidence-rail'){$wf='backgroundColor,completeColor,missingRequiredColor,blockedColor,optionalColor'-split',';if($by.Count-ne$wf.Count-or(Compare-Object ($by.Keys|sort) ($wf|sort))){throw 'evidence fields'};if($by.backgroundColor-cne'colors.surface-container'-or$by.completeColor-cne'colors.primary'-or$by.missingRequiredColor-cne'colors.tertiary'-or$by.blockedColor-cne'colors.error'-or$by.optionalColor-cne'colors.outline'){throw 'evidence tokens'};foreach($theme in 'light','dark'){foreach($field in 'completeColor','missingRequiredColor','blockedColor','optionalColor'){$key=$theme+'.'+$by[$field].Substring(7)+'|'+$theme+'.'+$by.backgroundColor.Substring(7);if($decl-cnotcontains$key){throw ('evidence pair '+$key)}}}}elseif($id-ceq'inspection-item-card'){$wf='backgroundColor,textColor,boundaryColor,boundaryAdjacentColor'-split',';if($by.Count-ne$wf.Count-or(Compare-Object ($by.Keys|sort) ($wf|sort))){throw 'item fields'};if($by.backgroundColor-cne'colors.surface-container'-or$by.textColor-cne'colors.on-surface'-or$by.boundaryColor-cne'colors.outline'-or$by.boundaryAdjacentColor-cne'colors.surface-container-low'){throw 'item tokens'};foreach($theme in 'light','dark'){foreach($p in @(@($by.textColor,$by.backgroundColor),@($by.boundaryColor,$by.boundaryAdjacentColor))){$key=$theme+'.'+$p[0].Substring(7)+'|'+$theme+'.'+$p[1].Substring(7);if($decl-cnotcontains$key){throw ('item pair '+$key)}}}}else{if(@($by.Keys|?{$_-cnotin @('backgroundColor','textColor')}).Count){throw ('unknown color field '+$id)};$bg=$by.backgroundColor;$fg=$by.textColor;if($fg-and$bg){if($fg-like'colors.*'-and$bg-like'colors.*'){foreach($theme in 'light','dark'){$key=$theme+'.'+$fg.Substring(7)+'|'+$theme+'.'+$bg.Substring(7);if($decl-cnotcontains$key){throw ('component binding '+$id+' '+$key)}}}elseif($fg-ceq'interaction.onCameraScrim'-and$bg-ceq'interaction.cameraScrim'){if($decl-cnotcontains'camera.on-scrim|camera.scrim-over-white'){throw ('component binding '+$id)}}else{throw ('unmapped color pair '+$id)}}elseif(-not$fg-and$bg-ceq'colors.outline-variant'-and$id-ceq'divider'){foreach($theme in 'light','dark'){$key=$theme+'.outline-variant|'+$theme+'.surface';if($decl-cnotcontains$key){throw ('component binding '+$id+' '+$key)}}}elseif(-not$fg-and$bg-ceq'colors.primary'-and$id-ceq'focus-indicator'){foreach($theme in 'light','dark'){$key=$theme+'.primary|'+$theme+'.surface';if($decl-cnotcontains$key){throw ('component binding '+$id+' '+$key)}}}else{throw ('incomplete color pair '+$id)}}};if($covered-ne18){throw ('component color coverage '+$covered)};foreach($bad in 'allowedComponents','componentContracts','200% font scale','Landscape is a supported fallback','Room panoramas default to the history overlay','opaque high-contrast scrim','Disabled content remains identifiable but is exempt from WCAG contrast. A disabled action always has adjacent text explaining why it is unavailable. Disabled styling never carries the only explanation.','Cards use a 1px `outline-variant` edge only when adjacent tones do not separate clearly.','Level 0 is the screen, level 1 is an active card, level 2 is a grouped region or bottom dock','for token in renderedForegroundFocusAndBoundaryTokens'){if($region.Contains($bad)){throw ('scope regression '+$bad)}};foreach($s in 'c_srgb = channel / 255','c_srgb <= 0.04045','c_srgb / 12.92','((c_srgb + 0.055) / 1.055) ^ 2.4','Tablet and landscape optimisation are outside the current UI card','Room panoramas may default to the history overlay; item photos do not.','worst case is a white preview composited to `#5C5C5C`, which gives `6.69:1` contrast','if metadata.bindings lacks exact (pair.foreground, pair.background): BUILD_FAIL(MISSING_METADATA)','Level 0 is the screen, level 1 is a grouped region or bottom dock, level 2 is an active card','Cards use a 1px `outline` edge only when adjacent tones do not separate clearly'){if(-not$region.Contains($s)){throw ('review fix '+$s)}};$sha=[Security.Cryptography.SHA256]::Create();function ExactHash($p,$e){$s=(Get-Content $p -Raw).Replace([string][char]13,'').TrimEnd();$a=([BitConverter]::ToString($sha.ComputeHash([Text.Encoding]::UTF8.GetBytes($s)))-replace'-','').ToLower();if($a-cne$e){throw ('exact card '+$p)}};ExactHash 'specs/tasks/T0-RECONCILE-DESIGN-FOUNDATIONS.md' '93058aedf396eaadaa3fdac837ba650dba7c48a08328658466bfff073fcf4aed';ExactHash 'specs/tasks/T0-RECONCILE-DESIGN-COMPONENTS.md' '9c040a551915ef58ff2a3746f3c7c130859a5bc85e33e2bb930c50271005c7ee'
dod_exit: 0
dod_assert: A1–A4；远端 tip/parent/stat、49 bindings、exact rendered pairs、dark-map、component/elevation/ratio 与两卡全文 hash 任一漂移即 RED
review_gate: codex {verdict:pass}
hygiene: 仅登记本轮 R3 精确配对/层级修订源与下游钉点
doc_sync: 无
---

# T0-RECONCILE-DESIGN-FOUNDATION-R3-PAIR-FIXTURE

只登记 PR 165 后续 R3 所需的精确渲染配对与 tonal 层级修订，不在本卡修改主设计正文。

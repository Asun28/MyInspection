---
id: T0-SCAFFOLD-CARD-CONTRACT-REPAIR
title: Apply five bounded pending-card contract corrections with a self-scoped admin card
status: todo
depends_on: []
branch: T0-SCAFFOLD-CARD-CONTRACT-REPAIR
worktree: C:\wt\T0-SCAFFOLD-CARD-CONTRACT-REPAIR
allow_paths:
  - specs/tasks/T0-SELFTEST-NIGHTLY-META.md
  - specs/tasks/T0-INIT-ASSIGNMENT-ANCHORS.md
  - specs/tasks/T0-TRIAGE-EVIDENCE-CASE.md
  - specs/tasks/T0-SCAFFOLD-SYNC-046.md
  - docs/TASK-BOARD.md
  - specs/tasks/T0-SCAFFOLD-CARD-CONTRACT-REPAIR.md
forbid:
  - Changing runtime scripts, workflows, product code, configuration, budgets, review counters, archive projections, or any path outside the six allow_paths
  - Changing pending-card acceptance, forbid, non_goals, dependencies or status except the exact listed metadata replacements
  - Treating this metadata repair result as implementation, RED/GREEN evidence, capability completion, or capability R3 evidence
  - Running a default-primary mutation or using an implicit candidate worktree
non_goals:
  - Implementing NIGHTLY, INIT, TRIAGE or SYNC046
  - Changing TRIAGE reporter behavior or adding a test runner
  - Adopting v0.47 behavior or adding a v0.47 ledger row
  - Modifying SCAFFOLD-ONLY, RISK, LOW, BUNDLE, any archive record, or any capability status value
diagnosis: Contract projections were updated without reconciling their implementation and release authorities: NIGHTLY omitted the shared routing helper; INIT described the future v0.46 ledger as its registration baseline although ceb2685e pins v0.45; TRIAGE used an always-zero reporter exit without inspecting its FAIL output; SYNC046 kept pre-v0.47 release wording and a host-dependent latest-tag assertion, and omitted the CLAUDE synchronization surface; TASK-BOARD retained the old TRIAGE author route and SYNC046 release wording.
acceptance:
  - "A1 The four pending cards and TASK-BOARD receive only the exact NIGHTLY helper-path, INIT historical-baseline, TRIAGE stdout-oracle, SYNC046 seven-transform, released-v0.47 SYNC046 board wording, and TASK-BOARD TRIAGE-route corrections."
  - "A2 NIGHTLY, INIT, TRIAGE and SYNC046 remain status todo; this management card is also todo and its result is not a capability implementation or completion claim."
  - "A3 The self-check validates this card's id, status and exact six-item allow_paths list; SYNC046 doc_sync names CLAUDE.md current/high-water references; runtime, archive and unrelated capability-status files remain unchanged."
dod_command: $ErrorActionPreference='Stop'; $b='1747a4de20e7e62c7205d83feb2a456ee242976e'; $nl=[string][char]10; $allowed=@('specs/tasks/T0-SELFTEST-NIGHTLY-META.md','specs/tasks/T0-INIT-ASSIGNMENT-ANCHORS.md','specs/tasks/T0-TRIAGE-EVIDENCE-CASE.md','specs/tasks/T0-SCAFFOLD-SYNC-046.md','docs/TASK-BOARD.md'); $self='specs/tasks/T0-SCAFFOLD-CARD-CONTRACT-REPAIR.md'; $allAllowed=@($allowed+$self); $read={param($p) [IO.File]::ReadAllText((Join-Path (Get-Location) $p)).Replace("`r`n","`n")}; $base={param($p) $x=(& git show ($b+':'+$p) | Out-String); if($LASTEXITCODE){throw ('base read failed: '+$p)}; $x.Replace("`r`n","`n")}; $replace={param($s,$pattern,$new,$label) $ms=[regex]::Matches($s,$pattern); if($ms.Count -ne 1){throw ('anchor count '+$label+'='+$ms.Count)}; [regex]::Replace($s,$pattern,[System.Text.RegularExpressions.MatchEvaluator]{param($m)$new},1)}; $expected=@{}; $n=& $base $allowed[0]; $expected[$allowed[0]]=& $replace $n '(?m)^  - scripts/selftest\.ps1\n' "  - scripts/selftest.ps1`n  - scripts/_validation.ps1`n" 'NIGHTLY helper path'; $i=& $base $allowed[1]; $expected[$allowed[1]]=& $replace $i 'Version-closeout coupling \(confirmed before R1/RED\): scaffold-sync''s existing real-ledger SelfCheck\npins current v0\.46\.0\.' "Version-closeout coupling: registration baseline ceb2685e9e3ada76a503377584d512c0c6d2af4d pins v0.45.0; recheck its config,`nauthority references and real-ledger SelfCheck against the actual remote tree before R1/RED." 'INIT historical baseline'; $t=& $base $allowed[2]; $oldTri='dod_command: pwsh -NoProfile -File scripts/triage.ps1 selfcheck'; $newTri='dod_command: $t=(& pwsh -NoProfile -File scripts/triage.ps1 selfcheck 2>&1 | Out-String); $x=$LASTEXITCODE; Write-Host $t; if ($x -ne 0 -or $t -cnotmatch ''(?m)^triage selfcheck: PASS(?=（|[ \t\r]|$)'' -or $t -cmatch ''(?m)^[ \t]*(?:triage selfcheck: FAIL\b|FAIL\b)'') { exit 1 }'; $expected[$allowed[2]]=& $replace $t '(?m)^dod_command: pwsh -NoProfile -File scripts/triage\.ps1 selfcheck$' $newTri 'TRIAGE stdout DoD'; $s=& $base $allowed[3]; $s=& $replace $s '(?m)^  - specs/tasks/T0-SCAFFOLD-SYNC-046\.md$' "  - specs/tasks/T0-SCAFFOLD-SYNC-046.md`n  - CLAUDE.md" 'SYNC046 CLAUDE path'; $s=& $replace $s '(?m)^  - Adopting unreleased upstream tiered-acceptance or meta-nightly work$' '  - Adopting the released v0.47.0 tiered-acceptance, nightly-meta, or other v0.47 coupling groups in this v0.46 card' 'SYNC046 v0.47 wording'; $s=& $replace $s '(?m)^dod_command: pwsh -NoProfile -File scripts/check-cards\.ps1 -TaskId T0-SCAFFOLD-SYNC-046;.*no newer upstream release.*$' 'dod_command: pwsh -NoProfile -File scripts/check-cards.ps1 -TaskId T0-SCAFFOLD-SYNC-046; if ($LASTEXITCODE) { exit 1 }; $self=(& pwsh -NoProfile -File scripts/scaffold-sync.ps1 selfcheck 2>&1 | Out-String); $selfExit=$LASTEXITCODE; Write-Host $self; if ($selfExit -or $self -cnotmatch ''(?m)^scaffold-sync selfcheck: PASS(?=[ \t\r]|$)'') { exit 1 }; . ./scripts/_config.ps1; $ledger=Get-Content -LiteralPath docs/SCAFFOLD-SYNC.md -Raw; $claude=Get-Content -LiteralPath CLAUDE.md -Raw; if ((Get-ScaffoldOriginVersion) -cne ''0.29.0'' -or (Get-ScaffoldVersion) -cne ''0.46.0'' -or $ledger -notmatch ''(?m)^\| v0\.46\.0 \| applied \|.*d0c9145970e69626318a26ce922650f1a631c2f0'' -or $claude -notmatch ''ScaffoldVersion.*v0\.46\.0'' -or $claude -notmatch ''当前版本为 \*\*v0\.46\.0\*\*'') { exit 1 }' 'SYNC046 local DoD'; $s=& $replace $s '(?m)^dod_assert: Origin remains 0\.29\.0, current becomes 0\.46\.0, the v0\.46\.0 ledger row names the exact release tag and local-equivalent decision, scaffold-sync selfcheck passes, and check reports no unevaluated release\.$' "dod_assert: Origin remains 0.29.0; current and the newest valid local ledger row are both v0.46.0, the row is an applied decision pinned to upstream commit/tag d0c9145970e69626318a26ce922650f1a631c2f0, CLAUDE.md current/high-water references are synchronized, and the existing scaffold-sync selfcheck passes both the real v0.46 ledger/origin-current contract and a Get-NewerVersion fixture that recognizes v0.47.0 above base v0.46.0 without adding v0.47.0 to this card's ledger." 'SYNC046 assertion'; $s=& $replace $s '(?m)^  - "A3 Released v0\.46\.0 behavior is accounted for without importing unrelated pre-tag history or unreleased tier/meta changes, and scaffold-sync selfcheck/check remain green"$' '  - "A3 Released v0.46.0 behavior is accounted for without importing unrelated pre-tag history or the released v0.47 tier/meta groups, and the existing scaffold-sync selfcheck remains green"' 'SYNC046 A3'; $s=& $replace $s '(?m)^hygiene: Metadata-only alignment uses the existing selfcheck and live high-water check; no duplicate test framework or copied upstream implementation\.$' 'hygiene: Metadata-only alignment uses the existing selfcheck and deterministic local high-water checks; no duplicate test framework or copied upstream implementation.' 'SYNC046 hygiene'; $s=& $replace $s 'doc_sync: docs/SCAFFOLD-SYNC.md decision ledger and this card status' 'doc_sync: docs/SCAFFOLD-SYNC.md decision ledger, CLAUDE.md current/high-water references, and this card status' 'SYNC046 doc_sync'; $expected[$allowed[3]]=$s; $d=& $base $allowed[4]; $oldSyncBoard='| W0 | T0-SCAFFOLD-SYNC-046 | 核对 upstream v0.46.0，并推进 origin/current 高水位账 | T0-SCAFFOLD-SYNC-045 | S | GPT-5.6 Luna · high | GPT-5.6 Terra · high | 本地已具备 v0.46 双版本行为；只登记发布版等价采用与精确 tag，不纳未发布 tier/meta 工作 |'; $newSyncBoard='| W0 | T0-SCAFFOLD-SYNC-046 | 核对 upstream v0.46.0，并推进 origin/current 高水位账 | T0-SCAFFOLD-SYNC-045 | S | GPT-5.6 Luna · high | GPT-5.6 Terra · high | 本地已具备 v0.46 双版本行为；只登记发布版等价采用与精确 tag，不纳入已发布 v0.47.0 tiered-acceptance/nightly-meta coupling 组 |'; $d=& $replace $d ([regex]::Escape($oldSyncBoard)) $newSyncBoard 'TASK-BOARD SYNC046 released wording'; $oldBoard='| W0 | T0-TRIAGE-EVIDENCE-CASE | triage 裁决证据身份、HEAD 绑定与失败可观测性 | T0-LESSONS-CAP-TRIAGE-SPLIT | S | GPT-5.6 Sol · high | Codex R3 | PR #137 R3 fix-forward；独立于 exact extraction；actual-root case、per-root SHA、unreadable/unknown finding |'; $newBoard='| W0 | T0-TRIAGE-EVIDENCE-CASE | triage 裁决证据身份、HEAD 绑定与失败可观测性 | T0-LESSONS-CAP-TRIAGE-SPLIT | S | GPT-5.6 Terra · high | Sonnet 5 · max | PR #137 R3 fix-forward；独立于 exact extraction；actual-root case、per-root SHA、unreadable/unknown finding |'; $d=& $replace $d ([regex]::Escape($oldBoard)) $newBoard 'TASK-BOARD TRIAGE route'; $expected[$allowed[4]]=$d; foreach($p in $allowed){if(-not(Test-Path -LiteralPath $p)){throw ('missing candidate path '+$p)};if((& $read $p) -cne $expected[$p]){throw ('exact base-to-candidate transform mismatch: '+$p)}}; $changed=@(& git -c core.quotepath=false diff --name-only --no-renames $b --); if($LASTEXITCODE){throw 'diff failed'}; $changed+=@(& git -c core.quotepath=false ls-files --others --exclude-standard); if($LASTEXITCODE){throw 'ls-files failed'}; if(@($changed|Sort-Object -Unique|Where-Object{$_ -cnotin $allAllowed}).Count){throw 'unexpected changed path'}; if(-not(Test-Path -LiteralPath $self)){throw 'management card missing'}; . ./scripts/_cards.ps1; $fm=Get-FrontMatter (& $read $self); if($null -eq $fm -or (Get-Scalar $fm 'id') -cne 'T0-SCAFFOLD-CARD-CONTRACT-REPAIR' -or (Get-Scalar $fm 'status') -cne 'todo'){throw 'management card id/status mismatch'}; $actualAllow=@(Get-YamlListItems $fm 'allow_paths'); if($actualAllow.Count -ne 6 -or ([string]::Join($nl,$actualAllow) -cne [string]::Join($nl,$allAllowed))){throw 'management card allow_paths mismatch'}; foreach($p in $allowed[0..3]){$x=& $read $p;if([regex]::Matches($x,'(?m)^status: todo$').Count -ne 1){throw ('pending status changed or ambiguous: '+$p)}}; Write-Host 'CARD-CONTRACT-REPAIR-PASS'
dod_exit: 0
dod_assert: The four capability cards and board equal the pinned review base after only the enumerated replacements; all four pending capability cards retain one status: todo; no unlisted path, runner, implementation, archive or completion claim is introduced.
review_gate: codex {verdict:pass}
hygiene: Metadata-only SkipRed admin card; use exact base-to-candidate comparison and existing card/index checks, while retaining normal verify, R3, CI and PR gates.
doc_sync: none; this is a bounded pending-card contract correction only.
---

# Administrative scope

This publishable card records the five pending-contract corrections and their management scope. The four capability cards remain pending their own implementation and acceptance. Use standard metadata SkipRed and all other normal project gates.

The authority sweep covered all references to these four pending cards in `CLAUDE.md`, `.claude/`, `docs/`, `specs/tasks/`, and `scripts/`. The relevant current projections are the four cards and the two board rows changed here. Existing RISK/META/SKILL card dependencies retain their meaning; no runtime or archived historical statement is advanced by this repair. `scripts/scaffold-sync.ps1` real-ledger selfcheck pins v0.45 at the exact base, and `scripts/_config.ps1`, `docs/SCAFFOLD-SYNC.md`, and the CLAUDE current-version references agree with that baseline. The v0.46 card keeps immutable origin v0.29 and excludes v0.47 adoption. TRIAGE help defines PASS output as its selfcheck result; the revised card checks that output while preserving the reporter's exit contract. The scoped exact-transform oracle covers both board rows and the CLAUDE allow/doc_sync projections together, so leaving either old projection unchanged fails acceptance.
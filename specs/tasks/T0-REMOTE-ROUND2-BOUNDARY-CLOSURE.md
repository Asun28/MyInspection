---
id: T0-REMOTE-ROUND2-BOUNDARY-CLOSURE
title: Close remote Boundary through its merged immutable evidence publication
status: merged
depends_on: [T0-REMOTE-BOUNDARY-EVIDENCE, T1-STORAGE-PATH-BOUNDARY-REMOTE]
allow_paths:
  - specs/tasks/T0-REMOTE-ROUND2-BOUNDARY-CLOSURE.md
  - specs/tasks/T1-STORAGE-PATH-BOUNDARY-REMOTE.md
  - specs/archive/tasks/T1-STORAGE-PATH-BOUNDARY-REMOTE.md
  - specs/archive/cards-index.md
  - CLAUDE.md
  - docs/TASK-BOARD.md
  - docs/SECURITY.md
  - docs/adr/0006-offline-security-backup-hardening.md
acceptance:
  - "A1 Pin the actual remotely merged T0-REMOTE-BOUNDARY-EVIDENCE whole Git blob and byte SHA; verify its saved formal publication lifecycle and rerun its entire pinned evidence core against all original proof. Reject a locally passed or changed predecessor. The archived portable record equals that published record exactly."
  - "A2 Preserve the full remote Boundary contract and local history; change only status to merged and append exactly one observed lifecycle receipt. Bind PR310, reviewed head, actual formal review and wrapper attempts, exact-head CI, merge, source pins, T35 RED lineage, T24 and guarded cleanup to copied original records."
  - "A3 Recompute 35 distinct actual compiling mutants from approved selectors and production bytes; require each designated primary AssertionError and preserve secondary exceptions. Parse all case nodes, identities and complete inventories for three forced 177-app/20-direct DoDs, raw exits/logs/APKs and source restoration. Final test differs only by appended receipt; POSIX remains unexecuted."
  - "A4 Use the complete actual remote base archive, preserve every old file byte-for-byte and add only this alias (N to N+1), remove only its active copy and regenerate index. Keep this closure active and every other active contract/status unchanged."
  - "A5 Update only CLAUDE current stage, Boundary Board row and 3/10 effective on this PR merge, SECURITY and ADR-0006 Boundary publication. Keep Policy, Android, Keystore and later-I/O claims pending. Six approved payloads, copied proof, card/index, exact eight paths and whole-base/committed/working/staged whitespace pass."
forbid:
  - Production, tests, scripts, configuration, schema, dependency, lessons, debt or unrelated card edits
  - Promoting local historical results to remote proof, or claiming Android or later-I/O acceptance
  - Direct master push, history rewriting, bypassed gates or increased diff limits
non_goals:
  - Storage Policy successor, Android adapter, Keystore, tenant migration or later product rounds
dod_command: $raw = Get-Content -LiteralPath 'specs/tasks/T0-REMOTE-ROUND2-BOUNDARY-CLOSURE.md' -Raw; $blocks = [regex]::Matches($raw, '(?ms)^```powershell\r?\n(.*?)^```[ \t]*$'); if ($blocks.Count -ne 1) { throw 'Expected one approved assertion block' }; & ([scriptblock]::Create($blocks[0].Groups[1].Value)); if ($LASTEXITCODE -ne 0) { exit 1 }
dod_exit: 0
dod_assert: Verify saved original runtime and lifecycle records, fixed payloads, complete archive preservation and exact candidate scope. This does not query live GitHub or rerun historical tests. Normal verify, formal R3, exact-candidate CI, merge and cleanup remain mandatory.
review_gate: codex {verdict:pass}
hygiene: Metadata uses explicit SkipRed. Disposable negatives must reject CI, cleanup, mutation, raw proof, payload/status, archive and committed whitespace changes.
doc_sync: This closure becomes effective only on its own PR merge; retain its own review/CI/merge/cleanup evidence. Keep this closure active until ordinary archival maintenance.
---

# Boundary remote R5 closure

Runtime execution and source pins are preserved. PR310 merged as 74aa9cb7ac6e70bbae30cb5d3a2024d8c95d6a0c from bfefc1057a0ecbdfde4f747193c68c0069bdb73c, with CI35174714509 successful on that head. Two normal wrapper attempts yielded one actual Sol/high code-review PASS: the first attempt was quota R3-NO-OUTPUT without a code verdict. The saved product rounds file remains 1; those product ship commands did not reset it. The original cleanup reported exit0/absence at 2026-09-17T23:34:57.6185122Z but its raw log retained deletion warnings without a raw post-list. The immutable evidence core checks the separately captured current absence at 2026-09-18T01:16:43Z. R5 remains pending this separate closure.

Evidence publication PR312 separately had three actual reviews (BLOCK, BLOCK, PASS) and one authorized counter reset. Its 02:10:29 cleanup also logged `Filename too long` and `not a working tree`; immediately captured filesystem and Git outputs prove the subsequent absence, without claiming those failed commands succeeded. All three ship attempts, reset authorization and original cleanup command script/outputs remain in the pinned prerequisite bundle.

The archive appendix must preserve the entire original alias with only status changed and append `portable-lifecycle.json` under one `<!-- boundary-remote-lifecycle-receipt -->` marker and JSON fence. Copy all cited original evidence into the reviewer-visible proof root below; an outer manifest covers every relative leaf, byte length and SHA-256, excluding itself. Child manifests and saved raw evidence are rechecked. Preserve every wrapper attempt and the separate actual-review count. Final source byte pins bind both reviewed and merged Git blobs; the earlier 390-line test body and final receipt-only appendix remain explicit.

The six LF-normalized payloads below are bound to this actual post-publication base. The complete archive changes from the measured N to N+1; every pre-existing archive byte remains unchanged. The independent whole-card authority is the latest own-card path commit on `refs/heads/master` in fixed `D:/Projects/MyInspection`. That commit must change only this card. Root publishes `approval.json` and the complete `approved-card.md` under fixed `D:/Projects/MyInspection/_local/rotating-card-orchestrator/boundary-own-card-approval/` after separately approving and committing the final bytes. The record must match the derived commit, committed blob, raw SHA and exact eight-path scope; the candidate and saved approved card must equal that blob byte-for-byte. The record cannot select another repository, ref or path, and an ignored SHA file is not an authority. Target status becomes effective only upon this closure PR merge. The complete candidate must remain within 750 changed lines / 45,000 characters before first ship, with the official hard limit of 1,000 / 60,000 unchanged.

The reviewer must independently inspect that original D path commit and the root authorization record, with its commit/blob/SHA values and complete approved raw card. The external root execution guard also compares these bytes. Candidate DoD code cannot establish its own authority if its checks are removed; original D master and root approval remain outside candidate write authority. No final self-hash or final approval-commit literal is embedded in this card. Unrelated master commits are allowed; a changed latest own-card commit requires renewed root approval.

This successor does not duplicate or abbreviate the evidence verifier. It first pins the complete predecessor card to its actual remote merge and invokes only that card's unique evidence core. It then compares the archived portable record exactly with the publication and performs every original archival, fixed-payload, eight-path scope, card/index and whitespace check below. The predecessor remains active and unchanged. The publication's own scope guard is deliberately not rerun against the successor's eight-file change.

The separate reviewer-visible `prerequisite/` saved-record copy must include the evidence publication's actual PR, review, exact-head CI, cleanup and copy manifest. These saved publication records are separate from the functional PR310 records. Follow the real source schemas when binding those records; never manufacture them from a local PASS. No product count changes until this closure itself passes and merges.

```powershell
$ErrorActionPreference='Stop'
$base='d53cec8c994189f98893f8ac25889bc00b281801'
$archiveBefore='197'
$evidenceMerge='00842ba9134bc592adc1c6a4569740d1fbe0bf36'
$evidenceBlob='b748fd9e250d29a440fb282d5e0156c49b54d14e'
$evidenceSha='20B479B8C3E3F4364875D019DD18F9BB864C543817962DD35C933F315DE4C0A0'
$evidencePath='specs/tasks/T0-REMOTE-BOUNDARY-EVIDENCE.md'
foreach($oid in @($base,$evidenceMerge,$evidenceBlob)){if($oid -notmatch '^[0-9a-f]{40}$'){throw 'Unresolved actual merged evidence handoff'}}
if($evidenceSha -notmatch '^[0-9A-F]{64}$' -or $archiveBefore -notmatch '^\d+$'){throw 'Unresolved approved evidence SHA or actual archive count'}
$archiveBefore=[int]$archiveBefore
$evidenceCardRaw=Get-Content -LiteralPath $evidencePath -Raw
$actualSha=[Convert]::ToHexString([Security.Cryptography.SHA256]::HashData([IO.File]::ReadAllBytes((Join-Path $PWD $evidencePath))))
if(![string]::Equals($actualSha,$evidenceSha,[StringComparison]::Ordinal)){throw 'Evidence card whole-byte SHA drift'}
$actualBlob=git hash-object --no-filters -- $evidencePath; if($LASTEXITCODE -ne 0 -or ![string]::Equals($actualBlob,$evidenceBlob,[StringComparison]::Ordinal)){throw 'Evidence card working blob drift'}
$mergedBlob=git rev-parse ('{0}:{1}' -f $evidenceMerge,$evidencePath); if($LASTEXITCODE -ne 0 -or ![string]::Equals($mergedBlob,$evidenceBlob,[StringComparison]::Ordinal)){throw 'Evidence card not bound to actual merged object'}
git merge-base --is-ancestor $evidenceMerge $base; if($LASTEXITCODE -ne 0){throw 'Evidence publication is not merged into actual closure base'}
if($evidenceCardRaw -cnotmatch '(?m)^status: merged\r?$'){throw 'Evidence card not published in merged state'}
$coreBlocks=[regex]::Matches($evidenceCardRaw,'(?ms)^```powershell\r?\n(.*?)^```[ \t]*$')
if($coreBlocks.Count -ne 1){throw 'Expected one complete published evidence core'}
. ([scriptblock]::Create($coreBlocks[0].Groups[1].Value))
git merge-base --is-ancestor $pr.mergeCommit.oid $base; Need ($LASTEXITCODE -eq 0) 'actual product merge precedes closure base'
# Reuse only the checked core. The predecessor's publication-only guard is not invoked here.
$publicationProof='_local/rotating-card-orchestrator/round2-r5-proof/boundary/prerequisite'
Manifest $publicationProof "$publicationProof/copy-manifest.json" '889B5B006DB4E291F88FFDBFA21E51828636D925F164BE486734FBF1BE1A5F33'
$publicationPr=J "$publicationProof/pr.json"; $publicationReview=J "$publicationProof/review.json"; $publicationCi=J "$publicationProof/ci.json"; $publicationCleanup=J "$publicationProof/cleanup-result.json"
Eq $publicationPr.state 'MERGED' 'actual evidence PR state'; Eq $publicationPr.mergeCommit.oid $evidenceMerge 'actual evidence PR merge'
Eq $publicationPr.number '312' 'approved evidence PR'; Eq $publicationPr.headRefOid '6047b36698b47bc3d070b6f4a370fbdae0aa7ed5' 'approved evidence reviewed head'
$reviewedBlob=git rev-parse ('{0}:{1}' -f $publicationPr.headRefOid,$evidencePath); Need ($LASTEXITCODE -eq 0) 'evidence reviewed card lookup'; Eq $reviewedBlob $evidenceBlob 'evidence exact reviewed card blob'
Eq $publicationReview.sha $publicationPr.headRefOid 'evidence review head'; Eq $publicationReview.verdict 'pass' 'evidence formal R3'; Need (@($publicationReview.reasons).Count -eq 0) 'evidence R3 reasons'
Eq $publicationReview.branch 'T0-REMOTE-BOUNDARY-EVIDENCE' 'evidence formal review branch'; Eq $publicationCi.databaseId '35297110734' 'approved evidence CI run'
Eq $publicationCi.headSha $publicationPr.headRefOid 'evidence CI head'; Eq $publicationCi.event 'pull_request' 'evidence CI event'; Eq $publicationCi.status 'completed' 'evidence CI status'; Eq $publicationCi.conclusion 'success' 'evidence CI result'
foreach($name in @('verify','required')){$js=@($publicationCi.jobs|Where-Object {$_.name -ceq $name}); Need ($js.Count -eq 1 -and $js[0].status -ceq 'completed' -and $js[0].conclusion -ceq 'success') 'evidence CI required jobs'}
Need ($publicationCleanup.exit -eq 0 -and $publicationCleanup.worktreeAbsent -ceq $true -and $publicationCleanup.branchAbsent -ceq $true) 'evidence guarded cleanup'; Eq $publicationCleanup.head $publicationPr.headRefOid 'evidence cleanup head'; Eq $publicationCleanup.merge $evidenceMerge 'evidence cleanup merge'
function PublicationCleanup([string]$folder,$record) {
    Eq (Sha "$folder/capture.ps1") '9B351B4D8B36F85B54C1CA902948F9BAAD3254C8C9DB7A8B78741704F5BD510A' 'publication cleanup command script'
    Timing $record; Eq $record.startedUtc '2026-09-18T02:10:26.6174911Z' 'publication cleanup start'; Eq $record.endedUtc '2026-09-18T02:10:29.7704858Z' 'publication cleanup end'
    Need ($record.worktreeListExit -eq 0 -and $record.branchListExit -eq 0) 'publication postcheck exits'; Need ($record.registrationAbsent -ceq $true) 'publication registration receipt'
    Need ([DateTimeOffset]::Parse($record.postcheckStartedUtc) -ge [DateTimeOffset]::Parse($record.startedUtc) -and [DateTimeOffset]::Parse($record.postcheckStartedUtc) -le [DateTimeOffset]::Parse($record.endedUtc)) 'publication postcheck time'
    $fs=J "$folder/filesystem.json"; Eq $fs.path 'C:/wt/T0-REMOTE-BOUNDARY-EVIDENCE' 'publication filesystem path'; Eq $fs.resolvedPath.Replace('\','/') $fs.path 'publication resolved path'
    Need ($fs.exists -ceq $false) 'publication filesystem absence'; Need ([DateTimeOffset]::Parse($fs.utc) -ge [DateTimeOffset]::Parse($record.postcheckStartedUtc) -and [DateTimeOffset]::Parse($fs.utc) -le [DateTimeOffset]::Parse($record.endedUtc)) 'publication filesystem time'
    Eq (Sha "$folder/cleanup.log") $record.cleanupLogSHA256 'publication cleanup raw hash'; $log=[IO.File]::ReadAllText("$folder/cleanup.log")
    Need ($log.Contains('Filename too long') -and $log.Contains('not a working tree') -and $log.Contains('T24-MERGETOKEN')) 'publication cleanup warnings retained'
    foreach($kind in @('worktrees','branch')) { Eq ([IO.File]::ReadAllText("$folder/$kind.stderr")) '' 'publication empty stderr' }
    $lines=@(Get-Content -LiteralPath "$folder/worktrees.stdout"); Need (@($lines|Where-Object {$_ -ceq 'worktree D:/Projects/MyInspection'}).Count -eq 1) 'publication raw worktree list'
    foreach($line in $lines){ Need (![string]::Equals($line.Replace('\','/'),'worktree C:/wt/T0-REMOTE-BOUNDARY-EVIDENCE',[StringComparison]::OrdinalIgnoreCase) -and ![string]::Equals($line,'branch refs/heads/T0-REMOTE-BOUNDARY-EVIDENCE',[StringComparison]::OrdinalIgnoreCase)) 'publication registration absent' }
    Eq ([IO.File]::ReadAllText("$folder/branch.stdout")) '' 'publication branch raw absence'
}
Eq (Sha "$publicationProof/root-cleanup/cleanup-result.json") (Sha "$publicationProof/cleanup-result.json") 'same original publication cleanup receipt'
PublicationCleanup "$publicationProof/root-cleanup" $publicationCleanup
$archive="specs/archive/tasks/$alias.md"; $archiveText=Get-Content -LiteralPath $archive -Raw
$archivedReceipt=[regex]::Matches($archiveText,'(?ms)^<!-- boundary-remote-lifecycle-receipt -->\r?\n```json\r?\n(.*?)\r?\n```[ \t]*$')
Need ($archivedReceipt.Count -eq 1) 'one archive portable receipt'
Eq $archivedReceipt[0].Groups[1].Value.Replace("`r`n","`n") $evidenceReceiptJson.Replace("`r`n","`n") 'archived portable exactly equals published observed receipt'

# Six fixed payloads; independent master authorizes the complete own card.
$payload=@{
 'specs/archive/tasks/T1-STORAGE-PATH-BOUNDARY-REMOTE.md'='F3B19F37E2D4B1F25E739D3B297002F0B9EA10B4395EAD1F81DEBD6E89419D84'
 'specs/archive/cards-index.md'='9E20EDD4D0EAF8C68232FF77BC92ACBC3CB1524A10FEE1F921894864216E0342'
 'CLAUDE.md'='9F897BCECC826D02390FC4DF00BD38834F32CD220383F33DE34799C7C4FACBC6'
 'docs/TASK-BOARD.md'='431A87AE7E62CD5A5DD3E2054EBCCCB132CCF16D565933941F27855F753DA8F0'
 'docs/SECURITY.md'='02BFB37E499B25CE9CFFECF025A605148DD48582B18087AF061859DB76EE0363'
 'docs/adr/0006-offline-security-backup-hardening.md'='94DCF995F2333B5F0640F687FFE3E9E5BEB9781C8E652A783ABAB49C2FD9EC5B'
}
foreach($p in $payload.Keys) { Eq (HText ([IO.File]::ReadAllText((Join-Path $PWD $p)).Replace("`r`n","`n"))) $payload[$p] "approved LF payload $p" }
# BEGIN independent own-card approval guard
$authority='D:/Projects/MyInspection'
$approvalRoot='D:/Projects/MyInspection/_local/rotating-card-orchestrator/boundary-own-card-approval'
$own='specs/tasks/T0-REMOTE-ROUND2-BOUNDARY-CLOSURE.md'
$approvalScope=@($own,'specs/tasks/T1-STORAGE-PATH-BOUNDARY-REMOTE.md','specs/archive/tasks/T1-STORAGE-PATH-BOUNDARY-REMOTE.md','specs/archive/cards-index.md','CLAUDE.md','docs/TASK-BOARD.md','docs/SECURITY.md','docs/adr/0006-offline-security-backup-hardening.md')
$approval=Get-Content -LiteralPath "$approvalRoot/approval.json" -Raw | ConvertFrom-Json -AsHashtable
Eq $approval.schemaVersion 1 'approval schema'; Eq $approval.task 'T0-REMOTE-ROUND2-BOUNDARY-CLOSURE' 'approval task'
Eq $approval.repository $authority 'fixed approval repository'; Eq $approval.ref 'refs/heads/master' 'fixed approval ref'; Eq $approval.path $own 'fixed approval path'
SetEq @($approval.allow_paths) $approvalScope 'approved eight-path scope'; Need ($approval.rootAuthorization -is [string] -and ![string]::IsNullOrWhiteSpace($approval.rootAuthorization)) 'root authorization evidence'
function ApprovalGit([string[]]$arguments) { $value=@(& git.exe -C $authority @arguments); Need ($LASTEXITCODE -eq 0) 'independent approval Git read'; return ($value -join "`n") }
$approvalCommit=ApprovalGit @('log','-1','--format=%H','refs/heads/master','--',$own)
Need ($approvalCommit -cmatch '^[0-9a-f]{40}$') 'latest master path commit'; Eq $approval.commit $approvalCommit 'approval record equals latest independent path commit'
& git.exe -C $authority merge-base --is-ancestor $approvalCommit refs/heads/master; Need ($LASTEXITCODE -eq 0) 'approval commit on independent master'
Need ((ApprovalGit @('rev-list','--parents','-n','1',$approvalCommit)).Split(' ').Count -eq 2) 'single-parent approval commit'
SetEq @((ApprovalGit @('diff-tree','--no-commit-id','--name-only','-r',$approvalCommit)).Split("`n")) @($own) 'approval commit changes only own card'
$approvalBlob=ApprovalGit @('rev-parse',('{0}:{1}' -f $approvalCommit,$own)); Need ($approvalBlob -cmatch '^[0-9a-f]{40}$') 'approval blob object'; Eq $approval.blob $approvalBlob 'record approval blob'
$start=[Diagnostics.ProcessStartInfo]::new(); $start.FileName='git.exe'; $start.UseShellExecute=$false; $start.CreateNoWindow=$true; $start.RedirectStandardOutput=$true; $start.RedirectStandardError=$true
foreach($arg in @('-C',$authority,'cat-file','blob',$approvalBlob)){[void]$start.ArgumentList.Add($arg)}
$process=[Diagnostics.Process]::new(); $process.StartInfo=$start; $buffer=[IO.MemoryStream]::new()
try { Need ($process.Start()) 'read approved raw blob'; $stderr=$process.StandardError.ReadToEndAsync(); $process.StandardOutput.BaseStream.CopyTo($buffer); $process.WaitForExit(); Need ($process.ExitCode -eq 0) 'approved blob read exit'; Eq $stderr.GetAwaiter().GetResult() '' 'approved blob read stderr'; $approvedBytes=$buffer.ToArray() } finally { $buffer.Dispose(); $process.Dispose() }
$approvalSha=[Convert]::ToHexString([Security.Cryptography.SHA256]::HashData($approvedBytes)); Eq $approval.sha256 $approvalSha 'record approved raw SHA'
Eq ([Convert]::ToBase64String([IO.File]::ReadAllBytes("$approvalRoot/approved-card.md"))) ([Convert]::ToBase64String($approvedBytes)) 'root approved raw card equals committed blob'
Eq ([Convert]::ToBase64String([IO.File]::ReadAllBytes((Join-Path $PWD $own)))) ([Convert]::ToBase64String($approvedBytes)) 'whole candidate equals independently committed approved card'
# END independent own-card approval guard
Need (!(Test-Path "specs/tasks/$alias.md")) 'active alias removed'; Need ($archiveText -cmatch '(?m)^status: merged\r?$') 'archived alias merged'
$oldLines=@(git show ('{0}:specs/tasks/{1}.md' -f $base,$alias)); Need ($LASTEXITCODE -eq 0) 'base alias present'; $old=($oldLines -join "`n")+"`n"
Need ([regex]::Matches($old,'(?m)^status: todo$').Count -eq 1) 'base alias status'; $prefix=[regex]::Replace($old,'(?m)^status: todo$','status: merged')
Need ($archiveText.Replace("`r`n","`n").StartsWith($prefix,[StringComparison]::Ordinal)) 'preserve entire active contract then append'
$before=@(git ls-tree -r --name-only $base -- specs/archive/tasks/); Need ($LASTEXITCODE -eq 0 -and $before.Count -eq $archiveBefore) 'full actual base archive count'
$after=@(Get-ChildItem 'specs/archive/tasks' -Force -File -Recurse|ForEach-Object {[IO.Path]::GetRelativePath($PWD.Path,$_.FullName).Replace('\','/')})
SetEq $after @($before+$archive) 'exact N to N+1 archive'
foreach($p in $before) { $oldBlob=git rev-parse ('{0}:{1}' -f $base,$p); Need ($LASTEXITCODE -eq 0) 'base archive object'; $newBlob=git hash-object --no-filters -- $p; Need ($LASTEXITCODE -eq 0) 'current archive bytes'; Eq $newBlob $oldBlob "unchanged full archive $p" }
$owned=@($own,"specs/tasks/$alias.md",$archive,'specs/archive/cards-index.md','CLAUDE.md','docs/TASK-BOARD.md','docs/SECURITY.md','docs/adr/0006-offline-security-backup-hardening.md')
$changed=[Collections.Generic.HashSet[string]]::new([StringComparer]::Ordinal)
foreach($args in @(@('diff','--no-renames','--name-only',$base,'--'),@('diff','--no-renames','--name-only',"$base...HEAD",'--'),@('diff','--cached','--no-renames','--name-only','--'),@('ls-files','--others','--exclude-standard'))) {
    $names=@(& git @args); Need ($LASTEXITCODE -eq 0) 'all candidate scope views'; foreach($p in $names){if($p){[void]$changed.Add($p)}}
}
SetEq @($changed) $owned 'exact eight candidate paths'
$activeBefore=@(git ls-tree -r --name-only $base -- specs/tasks/); Need ($LASTEXITCODE -eq 0) 'base active cards'
foreach($p in $activeBefore) { if($p -ceq "specs/tasks/$alias.md" -or $p -ceq $own){continue}; $blob=git rev-parse ('{0}:{1}' -f $base,$p); Need ($LASTEXITCODE -eq 0) 'base active blob'; $now=git hash-object --no-filters -- $p; Need ($LASTEXITCODE -eq 0) 'current active blob'; Eq $now $blob 'other active contracts/status unchanged' }
Need ((Get-Content $own -Raw) -cmatch '(?m)^status: merged\r?$') 'closure reviewed target status'
Need ((Get-Content docs/TASK-BOARD.md -Raw) -match '3/10') 'Boundary target product total'
pwsh -NoProfile -File scripts/check-cards.ps1; Need ($LASTEXITCODE -eq 0) 'card syntax'
pwsh -NoProfile -File scripts/archive.ps1 -CheckCardsIndex -Quiet; Need ($LASTEXITCODE -eq 0) 'generated full index'
git merge-base --is-ancestor $base HEAD; Need ($LASTEXITCODE -eq 0) 'actual base ancestor'
git diff --check $base; Need ($LASTEXITCODE -eq 0) 'whole base to candidate whitespace'
git diff --check "$base...HEAD"; Need ($LASTEXITCODE -eq 0) 'committed whitespace'
git diff --check; Need ($LASTEXITCODE -eq 0) 'working whitespace'
git diff --cached --check; Need ($LASTEXITCODE -eq 0) 'staged whitespace'
Write-Output '[R5-BOUNDARY-CLOSURE-PASS] Saved original proof and approved candidate verified; no historical tests rerun.'


```
